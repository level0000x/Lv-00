import React from 'react';
import ReactDOM from 'react-dom/client';
import { App } from '../L4-shell/App';
import '../L1-base/theme.css';

ReactDOM.createRoot(document.getElementById('root')!).render(<App />);
