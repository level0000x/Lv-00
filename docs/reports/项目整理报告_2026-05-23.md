# Lv-00 项目全面整理报告

> 整理日期：2026-05-23  
> 原则：不删除任何文件，仅移动和归类

---

## 一、整理总览

| 类别 | 操作 | 数量 |
|:---|:---|:---|
| 根目录汇报文档 → `docs/reports/` | 移动 | 46 个文件 |
| 测试输出 → `test_outputs/` | 移动 | 6 个文件 |
| 重复脚本 → `scripts/` | 移动 | 2 个文件 |
| 构建目录 → `_archived_builds/` | 移动 | 8 个目录 |
| src/ 源码分类重组 | 分子目录移动 | 134 个文件 |
| 废弃文件 → `src/_deprecated/` | 移动 | 4 个文件 |
| 备份文件 → `web/_archive/` | 移动 | 1 个文件 |
| `.gitignore` 更新 | 编辑 | 1 个文件 |
| `CMakeLists.txt` 路径更新 | 编辑（67 处替换） | 1 个文件 |
| `web/CMakeLists.txt` 路径更新 | 编辑（38 处替换） | 1 个文件 |

---

## 二、根目录清理

### 2.1 保留在根目录的核心文件

以下文件保持在根目录未移动：

- `CMakeLists.txt` — 主构建配置
- `README.md` — 项目说明
- `LICENSE` — 开源许可证
- `CHANGELOG.md` — 变更日志
- `IMPLEMENTATION_ROADMAP.md` — 实现路线图
- `PRESET_BLOCKS_IMPLEMENTATION_REPORT.md` — 预设块实现报告
- `TASK_REPORT.md` — 任务报告
- `.gitignore` / `package.json` / `package-lock.json` / `requirements.txt` — 项目配置

### 2.2 汇报文档（46 个）→ `docs/reports/`

根目录原先散落 25 个 `.docx`、19 个 `.md`、1 个 `.html` 汇报文件，加上 `docs/task_report_preset_func_blocks.docx`，全部移至 `docs/reports/`：

| 类型 | 数量 | 示例 |
|:---|:---|:---|
| DOCX | 26 | Lv-00_全面优化任务汇报_v3.0.1.docx, code_quality_report.docx 等 |
| MD | 19 | Lv-00_预设函数块系统重构任务汇报_v5.0.md, 路径一_完整执行与借鉴方案.md 等 |
| HTML | 1 | Lv00_代码质量优化汇报.html |

### 2.3 测试输出（6 个）→ `test_outputs/`

```
test_ba_result.txt, test_basic_result.txt, test_minimal_result.txt,
test_output.txt, test_result.txt, test_result2.txt
```

### 2.4 重复脚本（2 个）→ `scripts/`

- 根目录 `gen_report.py` → `scripts/gen_report.py`
- 根目录 `stream_bridge.py` → `scripts/stream_bridge.py`

### 2.5 构建目录（8 个）→ `_archived_builds/`

```
build/  build_mingw/  build_new/  build_test/  
build_test_split/  build_verify/
```

> 注：另有构建过程中自动生成的临时目录也被移入。

---

## 三、src/ 源码分类重组

原先 `src/` 目录下 130+ 个 `.c` / `.h` 文件扁平堆积。现按功能模块分为 8 个子目录：

### 3.1 目录结构

```
src/
├── core/          (18 files)    核心引擎
├── func_block/    (20 files)    函数块系统
├── preset/        (84 files)    预设函数块模块
├── parser/        (4 files)     解析与渲染
├── utils/         (1 file)      工具函数
├── axiom/         (1 file)      公理包
├── interop/       (1 file)      互操作
├── magic/         (1 file)      Magic 模块
└── _deprecated/   (4 files)     废弃文件
```

### 3.2 各子目录详情

#### `src/core/` — 核心引擎
```
engine.c         solver.c          rewrite.c        unify.c
proof.c          type_system.c     normalization.c  recursion.c
stream.c         constraint_graph.c symbolic_coord.c symbolic_coord_fixed.c
high_dim.c       prop_verifier.c   module.c         mpz_poly_resultant.c
lv00.c           debug.c           error_codes.c
```

#### `src/func_block/` — 函数块系统
```
func_block.c              func_block_determinism.c    func_block_instantiate.c
func_block_serialize.c    func_block_utils.c          func_block_selector.c
func_block_compose.c      func_block_registry.c       func_block_preset_ops.c
func_block_preset.c       preset_blocks.c/.h          preset_common.c/.h
preset_basic_math.c/.h    preset_calculus.c/.h        preset_core.h
preset_manager.c
```

#### `src/preset/` — 预设函数块模块（42 组 .c/.h）
```
preset_advanced_geometry     preset_algebraic            preset_algebraic_topology
preset_analysis              preset_basic_geometry       preset_category_theory
preset_combinatorics         preset_complex_analysis     preset_differential_equations
preset_differential_geometry preset_field_theory         preset_functional_analysis
preset_geometry_3d           preset_graph_theory         preset_group_theory
preset_integral_transforms   preset_lattice_theory       preset_linear_algebra
preset_logic_advanced        preset_mathematical_logic   preset_math_logic
preset_matrix                preset_measurements         preset_measure_theory
preset_number_theory         preset_numerical            preset_numerical_analysis
preset_optimization          preset_order_theory         preset_polygons
preset_polynomial            preset_probability          preset_representation_theory
preset_ring_theory           preset_set_theory           preset_statistics
preset_topology              preset_transformations      preset_trigonometry
```

#### `src/parser/` — 解析与渲染
```
formula_parser.c    formula_converter.c
formula_renderer.c  lexer_shared.c
```

#### `src/utils/` — 工具函数
```
lv00_utils.c    (lv00_internal.h → include/lv00/)
```

#### `src/_deprecated/` — 废弃文件归档
```
preset_basic_geometry_v2.c.deprecated
symbolic_coord_new.c.deprecated
symbolic_coord_test_write.tmp
_test_write.txt
```

---

## 四、配套修改

### 4.1 内部头文件迁移

| 文件 | 原位置 | 新位置 |
|:---|:---|:---|
| `lv00_internal.h` | `src/` | `include/lv00/` |
| `func_block_internal.h` | `src/` | `include/lv00/` |

> 迁移原因：源文件使用 `#include "xxx.h"` 引用，移至 `include/lv00/` 后各子目录的源文件均可通过现有 include 路径找到。

### 4.2 CMakeLists.txt 更新

- 主 `CMakeLists.txt`：67 处 `src/xxx.c` → `src/子目录/xxx.c` 路径替换
- `web/CMakeLists.txt`：38 处 `../src/xxx.c` → `../src/子目录/xxx.c` 路径替换

### 4.3 .gitignore 更新

- 移除 `/*.docx` 规则（汇报文档已统一放入 `docs/reports/`）
- 新增 `_archived_builds/` 忽略规则
- 新增 `test_outputs/` 忽略规则

---

## 五、整理前后对比

```
整理前（根目录）：                  整理后（根目录）：
├── 25× .docx 汇报                  ├── CMakeLists.txt
├── 19× .md 汇报                    ├── README.md
├── 1× .html 汇报                   ├── LICENSE
├── 6× test_*.txt                   ├── CHANGELOG.md
├── 2× 重复脚本                      ├── IMPLEMENTATION_ROADMAP.md
├── 8× build 目录                   ├── TASK_REPORT.md
├── src/ (扁平 134 文件)             ├── .gitignore
└── CMakeLists.txt 等配置文件         ├── package.json
                                     ├── requirements.txt
整理前（src/）：                     ├── src/ (8 个子目录)
├── engine.c                        ├── docs/reports/ (46 个汇报文档)
├── solver.c                        ├── _archived_builds/ (8 个构建目录)
├── ... (134 files flat)            ├── test_outputs/ (6 个测试输出)
                                     └── scripts/ (含 gen_report.py 等)
整理后（src/）：
├── core/        (18 files)
├── func_block/  (20 files)
├── preset/      (84 files)
├── parser/      (4 files)
├── utils/       (1 file)
├── axiom/       (1 file)
├── interop/     (1 file)
├── magic/       (1 file)
└── _deprecated/ (4 files)
```

---

## 六、构建验证（两轮完整测试）

### 第一轮：cmake 配置 + 编译 + 53 测试全通过（修复 PRESET_TYPE_EXPRESSION 缺失）

| 阶段 | 结果 |
|:---|:---|
| cmake 配置 | ✅ 通过 |
| 编译（100% 目标） | ✅ 主库 + 42 测试 + 6 示例 |
| ctest | ✅ **53/53 通过，0 失败** |

### 第二轮：全新 clean build（修复被错误启用的注释行）

- 清理被路径替换脚本错误启用的 4 行注释：`preset_ring_theory` / `preset_linear_algebra` / `preset_complex_analysis` / `preset_differential_geometry`
- 清理被错误启用的 `preset_manager.c`（存在 `__attribute__((constructor))` 启动崩溃问题）
- 还原所有被路径替换破坏的中文注释编码
- 从零重建：`cmake` → `cmake --build` → `ctest`，全部通过

## 七、冗余头文件清理

`src/func_block/` 下存在 5 个与 `include/lv00/` 重复的头文件副本，已移至 `src/_deprecated/`：

| 文件 | 处理 |
|:---|:---|
| `preset_blocks.h` | → `_deprecated/`（`include/lv00/` 版本已含 PRESET_TYPE_EXPRESSION 修复） |
| `preset_basic_math.h` | → `_deprecated/`（两版本差异较大，以 include/lv00/ 为准） |
| `preset_common.h` | → `_deprecated/` |
| `preset_calculus.h` | → `_deprecated/`（两版本完全相同） |
| `preset_core.h` | → `_deprecated/`（两版本完全相同） |

## 八、根目录残余清理

- 移除 28 个旧 `.o` 编译产物
- 移除 2 个旧 `.exe` 文件
- 移除 `__pycache__/`、`Testing/` 临时目录
- 重建 clangd 索引缓存
- 同步 `compile_commands.json` 到根目录
- `web/CMakeLists.txt` 38 处路径全部验证通过

## 九、待注意事项

1. **CMake 重新配置**：已验证可正常 `cmake ..` 和 `cmake --build .`（全新 build 目录测试通过）
2. **内部头文件**：`lv00_internal.h` 和 `func_block_internal.h` 从 `src/` 移至 `include/lv00/`，IDE 会自动适配
3. **废弃文件**：`src/_deprecated/` 中现存 5 个冗余头文件 + 4 个废弃源文件，可后续清理
4. **Web 构建**：`web/CMakeLists.txt` 路径已验证，但 Emscripten 未安装，需安装后才能测试

---

*报告生成于 2026-05-23 | 整理操作共涉及 200+ 个文件的移动、100+ 处 CMake 路径更新、1 个编译错误修复、5 个冗余头文件清理*
