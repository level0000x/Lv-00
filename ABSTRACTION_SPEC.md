# Lv-00 抽象规范（Abstraction Specification）

> 版本: 1.0
> 生效日期: 2026-08-09
> 适用范围: Lv-00 几何推理内核全量 C 代码（`core/`）、测试代码（`test/`）、示例（`examples/`）
> 约束词: 本规范采用 RFC 2119 措辞。**必须**（MUST）表示强制要求；**应**（SHOULD）表示默认行为，偏离需在决策登记中记录理由；**可**（MAY）表示允许；**禁止**（MUST NOT）表示绝对不允许。

---

## 0. 范围与术语

### 0.1 目标

本规范定义 Lv-00 中"何时抽象、抽象什么、如何判定同构、如何落地、如何防回潮"的判定规则，使抽象决策可判定、可审计、可回滚。

### 0.2 非目标

本规范不涉及：性能优化、形式化验证、技术栈选型、领域语义设计。上述事项按各自专章处理，本规范仅以"语义一致性"为判据引用领域规则。

### 0.3 术语

| 术语 | 定义 |
|------|------|
| 抽象 | 将 ≥2 处重复的控制流 / 数据结构 / 语义常量的共同形态收敛到唯一设施 |
| 设施 | 抽象产物：一个函数、一组函数、一个类型 + 配套操作，或一个编译期常量 |
| 调用点 | 在设施之外使用 / 引用该设施的一处代码位置 |
| 骨架 | 一段代码的控制流嵌套结构（循环 / 分支 / 顺序的层次关系） |
| 叶子 | 骨架节点上的具体内容：常量值、比较谓词、访问路径、回调实现 |
| 事实同构 | 两段代码骨架逐段对应，差异全部可被参数表吸收 |
| 泛化同构 | 两个骨架不同的实现，各自是某个共同超概念的实例 |
| 超概念 | 以精确语义契约命名的抽象层，其所有实例共享同一输入输出语义 |
| 语义契约 | 设施对"做什么、不做什么、边界行为、失败行为"的正式描述 |
| 语义差异 | 合并后会导致任一侧输入输出语义改变的差异 |
| 豁免区 | 经判定不得抽象、必须保留各自实现的代码区域，需显式标注 |

### 0.4 总纲

**抽象的目标不是消灭重复代码，而是把同一语义的决策点收敛到唯一位置。**

一切抽象行为必须通过第 1–7 章的全部适用检查；检查未通过的候选，禁止实施。

---

## 1. 抽象时机

### 1.1 判据 A：控制流单例

**定义**：同一结构算法（遍历 / 搜索 / 排序 / 判定 / 消元）的骨架在 ≥2 处出现。

**识别方法**：对算法标志性 token 进行全库 grep（如 `indegree`、`while (front != NULL)`、`fabs(`），命中 ≥2 处即进入候选。

**判定规则**：
- 命中判据 A，且通过第 2 章同构判定者，**必须**抽象。
- 已有实例：`solver_order.c` 双份 Kahn → `kahn_priority_order`；17 个测试的手写 `fabs(a-b)<1e-10` → `approx_eq`。

### 1.2 判据 B：数据成长 / 布局模式重复

**定义**：手写 realloc 倍增、手写 capacity 管理、手写线性查找表、手写增量扩容在 ≥2 处出现。

**识别方法**：grep 裸 `realloc(` 调用点，排除容器设施自身实现。

**判定规则**：
- 命中判据 B 者**必须**收敛到统一容器设施。
- 已有实例：`formula_ast.c` / `gappa_propagate.c` / `prop_verifier_engine.c` / `geo_visual_complete.c` 的 +1 realloc → `lv_ensure_capacity`；`prop_verifier` memo 线性搜索 → `lvHashtable`。

### 1.3 判据 C：语义常量重复

**定义**：同一魔法值在多文件分别定义，或以裸字面量出现 ≥2 处。

**识别方法**：grep `1e-` 前缀字面量、`1e-6` / `1e-12` 等比较阈值。

**判定规则**：
- 命中判据 C 者**必须**收敛为具名编译期常量。
- 已有实例：裸 epsilon → `lv_EPSILON_SUPERTINY` / `lv_GEO_COLLINEAR_EPSILON`；裸 `1e-12` → `lv_is_integer_double`。

### 1.4 判据 D：枚举 ↔ 字符串双份维护（2026-08-09 批次 I 实证，正文补记）

**定义**：同一枚举集合的符号名 / 显示名 / 反查表在多处以独立手写数组或独立 `switch` 维护 ≥2 份，任一份增删枚举值时其余份必然失步。

**识别方法**：对同名字符串常量做全库 grep（如 `"collinear"`、`"ADD"`），命中 ≥2 处独立表 / `switch` 即进入候选；枚举值变更后逐一比对各份表项数。

**判定规则**：命中判据 D 者**必须**收敛为单一 X-macro 列表（见判据 F）或单一共享表；双份表项不一致（如一份缺 `collinear`）属于缺陷实现，按 2.3 终审二处理（以正确语义为唯一语义 + 回归测试）。

**已有实例**：比较操作符表 ×2 → `s_compare_op_strings`；关系词反查表 ×3 中一份缺 collinear → 共享 `lv_geometry_relation_keywords`；错误码类别映射 ×2 → `LV_ERROR_CATEGORIES_X`。

### 1.5 判据 E：并行后端家族（2026-08-10 批次 K）

**定义**：同一语义的多个后端实现（精确 / 数值 / 启发式）各自重复同一套「句柄 / 上下文有效性守卫」「入口状态机」「降级策略」样板，家族内每文件重复 ≥2 处。

**识别方法**：grep 后端家族目录中逐字同构的 `if (ctx == NULL) return`、id 范围检查、注册表持有性检查。

**判定规则**：命中判据 E 者**必须**抽取 header-only 守卫 / 共享状态机；谓词须为纯函数（不设错误上下文、不加锁、不写寄存器）。

**已有实例**：`groebner_engine*.c`（ring/poly/ideal/variety/engine 5 文件）逐字同构守卫 → `groebner_engine_guard.h`（`groebner_id_in_range` / `groebner_registry_has_ring`）。

### 1.6 判据 F：枚举元数据表家族（2026-08-10 批次 K）

**定义**：枚举 ↔ 字符串 / 名称数组 / 分发表等元数据在 ≥3 处按同一「X 列表宏 → 生成器宏」模式生成，或 ≥3 处手写同构的「边界检查 + NULL 槽 + 调用」分发样板。

**识别方法**：grep 手写 `{ "str", ENUM }` 初始化表、`switch (e) { case ...: return "..." }`、`if ((unsigned)k < N && table[k]) table[k](...)`。

**判定规则**：命中判据 F 者**必须**收敛为 lv_xmacro.h 的生成器族（`lv_XMACRO_ENUM` / `lv_XMACRO_TO_STR` / `lv_XMACRO_TO_ENUM_TABLE` / `lv_XMACRO_TO_NAME_ARRAY`）+ `LV_DISPATCH` / `LV_DISPATCH_VOID`；新增枚举值只改 X 列表一处。与判据 D 的关系：D 是「失步缺陷」，F 是「单源生成族」，D 的修正手段就是 F 的设施。

**已有实例**：lv_xmacro.h 全族 30 处 / 19 文件；`LV_DISPATCH(_VOID)` 收敛 ~30 处手写「边界 + NULL 槽 + 调用」三行样板。

### 1.7 判据 G：析构 shim / 字段销毁表（2026-08-10 批次 K）

**定义**：同一结构体的逐字段析构（释放子对象 → 释放外壳）在多模块重复；或 `(void *)obj` → `destroy((T *)obj)` 的强转样板重复。

**识别方法**：grep `lv_FIELD_` / `lv_obj_destroy_fields` 使用点，及手写「先 destroy 子字段再 free 自身」的成对代码块。

**判定规则**：命中判据 G 者**必须**收敛为 `lv_obj_destroy_fields` 字段描述表；析构函数指针用 `LV_DESTROY_SHIM` 生成（保持字段类型指针 → void 指针 shim 签名，不改变公共 ABI）。

**已有实例**：`LV_DESTROY_SHIM` 20 处 / 12 文件；magic_array / magic_rune / magic_spell / magic_domain / constraint_graph / context / engine_lifecycle 等字段表。

### 1.8 判据 H：维度展开重复项（2026-08-10 批次 K）

**定义**：同一坐标 / 多项式 / 方程的「维度展开 + 提取」逻辑（对每个坐标分量重复同一处理）在多处复制，仅维度上限或分量名不同；或共享资源的「分配 + 归还」配对契约散落各文件。

**识别方法**：grep 连续 `coords[0]` `coords[1]` 手写展开、坐标提取循环、方程系统 push 样板、同一池的 init/clear 调用对。

**判定规则**：命中判据 H 者**必须**收敛为共享提取子模块 / 共享池；资源配对（init 失败内部归还、push 后无条件归还）须以书面契约钉住。

**已有实例**：solver.c 坐标 / 方程提取 → `solver_coord_extract.c`（`extract_equations_from_constraints` / `solver_extract_equations_full`）；coeff_pool 池生命周期配对契约（C2-3 标注）。

### 1.9 判据 I：数字提取样板（2026-08-11 批次 N 实证）

**定义**：从嵌入文本中提取数值的「关键词定位（strstr）→ 回退数字起始 → 解析整数」三连样板在多处重复；嵌入文本（如 `"12尝试…"`）使整串严格解析语义必然失败，只有前缀解析语义正确。

**识别方法**：grep `strstr(` 后随反向 while 扫描数字（`*(num_start - 1) >= '0'`）+ `lv_parse_int` 的三连模式。

**判定规则**：命中判据 I 者**必须**收敛为 `lv_parse_int_before(base, pos, &out)`（lv_parse_utils.h，静态内联）：自 pos 向左扫描连续数字并以 strtol 前缀语义解析；无数字 / 溢出 / 参数非法返回 -1 且不写入 out。与 `lv_parse_int`（整串严格语义）互补，二者语义契约以 DOX 注释钉住。

**已有实例**：meta_verify.c 4 处（策略尝试数 / 标记数 / 几何对象数 / 字节数）；test_meta_verify.c `test_parse_int_before` 覆盖正常 / 边界（base 处读取）/ 无数字 / 溢出 / NULL 参数。

### 1.10 判据 J：字符串安全复制 / 拼接样板（2026-08-11 批次 N 实证）

**定义**：手写「strncpy + 手动 NUL 终止」或裸 `strncpy(` 拷贝样板在多文件重复，是 NUL 终止遗忘 / 截断语义分歧的高发点。

**识别方法**：grep 裸 `strncpy(`、手写 `[dest_size - 1] = '\0'` 尾随行。

**判定规则**：命中判据 J 者**必须**收敛为 `lv_strlcpy`（保证 NUL 终止，返回实际复制字符数）/ `lv_strlcat`（lv_utils.h）；新增字符串复制一律走 `lv_strlcpy`，裸 `strncpy(` 加入治理黑名单。

**已有实例**：全库 336 处 `lv_strlcpy` / `lv_strlcat` 调用点；core 内裸 `strncpy(` 归零（grep 验证）。契约卡：`lv_strlcpy` 必须写明"保证 NUL 终止"（与 `strncpy` 的关键差异，见 4.4）。

### 1.11 判据 K：错误结果样板（2026-08-11 批次 N 实证）

**定义**：结果结构体（success + error_msg）的「置失败 + 写消息」双行样板 `result.success = 0; lv_strlcpy(result.error_msg, msg, sizeof(result.error_msg));` 在多模块重复。

**识别方法**：grep `\.success = 0;` 后随 `lv_strlcpy(result.error_msg, ...)` 的连续行。

**判定规则**：命中判据 K 者**必须**收敛为 `lv_RESULT_FAIL(res, msg)` 宏（lv_error.h）：等价双行样板，NULL 消息写空串，不包含 return（调用方保留自己的 return 语句）；宏内 `sizeof` 需要完整结构类型。**豁免**（禁止套用本宏）：① 格式化消息样板（`snprintf` 形态，如 block_scheduler 环检测消息，代码处已标注 `/* exempt: */`）；② `lvParseResult::errors[]` 数组形态（error_count + 多槽，见 lv_parser.h）。两豁免均须在代码处显式标注 `/* exempt: */` 并登记到第 9 章。

**已有实例**：5 模块 34 处迁移（block_scheduler 8 / block_to_node 11 / block_to_text 5 / block_to_geometry 9 / representation_converter 1 + `make_error_result` 辅助函数收敛）；"Out of memory" → `lv_ERR_MSG_OOM` 规范文本收敛；test_error_handling.c `test_result_fail_macro`（正常 / NULL / 覆盖 / 截断 / 边界）。

### 1.12 判据 L：snprintf 定长复制 / 写入样板（2026-08-13 批次 U 实证）

**定义**：以 `snprintf` 完成「定长字符串复制」或「返回值截断防御」的样板在多文件重复，是缓冲区大小误配、NULL 源未定义行为、截断检测遗漏的高发点。两个子形态：

- **L1**：`snprintf(dst, size, "%s", src)` 语义等价 `lv_strlcpy`（返回源串长度，保证 NUL 终止）。`dst` 为定长缓冲区基址、`src` 为普通字符串时，该形态是伪复制，**必须**直接用 `lv_strlcpy`。
- **L2**：`snprintf` 后手动返回值防御（`if (n < 0 || n >= size)`）以检测截断的样板，**应**收敛为带防御的 `lv_snprintf` 包装（待设施落地后实施）。

**识别方法**：grep `snprintf(..., "%s", ...)` 纯复制形态（L1）；grep `snprintf(` 后随返回值防御的连续行（L2）。

**判定规则**：L1 命中者**必须**收敛为 `lv_strlcpy`，调用方按返回值用途 `(int)` 强转 `size_t`。**豁免**：① `dst` 为 `lv_str_trim` 等返回的偏移指针、`size` 用基址 `sizeof` 者（非纯复制）；② 游标追加形态 `snprintf(buf + w, rem, "%s", ...)`（归入游标缓冲链，批次 U3）。L2 待设施落地后实施。

**已有实例**：L1 全库 33 处命中，30 处纯复制迁移为 `lv_strlcpy`（18 文件），3 处豁免（mini_kernel 1 偏移指针 + interactive_geo 2 游标追加）。

### 1.13 粒度门槛

同时满足以下全部条件才允许抽象；缺一**禁止**抽象，记入决策登记待办：

1. **真实调用点 ≥ 2**：现在库内就在使用，禁止为未来假设预支抽象。
2. **行为等价可验证**：能用测试证明新旧输出逐位 / 逐行为一致。
3. **语义无冲突**：不触碰第 3.3 节豁免区。

---

## 2. 同构判定

### 2.1 事实同构：差异分类法

对候选 A、B，逐段对齐后列出全部差异，每个差异归入下表：

| 差异类型 | 判定 | 处理 |
|----------|------|------|
| 常量差异 | 仅数值不同（epsilon、容量初值、超时） | 事实同构，常量参数化吸收 |
| 回调差异 | 同一骨架位置调用的函数不同（比较器、谓词、访问器） | 事实同构，回调参数化吸收 |
| 结构差异 | 多一层循环、少一个分支、迭代顺序不同 | 先尝试重新对齐；对齐失败则按 2.3 终审一处理 |
| 语义差异 | 数值语义 / 类型本质不同 | 伪同构，豁免（第 3.3 节） |

**规则**：差异全部为常量 / 回调差异者，**必须**抽象；出现语义差异者**禁止**合并。

### 2.2 泛化同构：超概念检验

适用场景：两个骨架不同，但都是某个共同超概念 C 的实例。必须依次通过三步检验：

1. **命名检验**：C 的命名必须精确到语义契约级别（如"图上确定性排序"、"浮点近似比较"、"增长策略"）。禁止语义过宽的命名（如"处理数据"）——过宽命名视为检验不通过。
2. **映射检验**：S1、S2 的控制流必须各自是 C 的参数 / 回调 / 子步骤序列的实例，任一实例需要 C 提供额外的新控制流才能表达即视为不通过。
3. **扩展性检验**：任取第三个候选 S3（库内真实存在或已命名的明确需求）。C 无需修改、无需新增分支即可表达 S3 方为通过；否则 C 为拼凑产物，**禁止**建立。

**规则**：泛化后同构的特例数门槛高于事实同构：**≥ 3 个特例**，或 **2 个特例 + 1 个明确命名的第三候选**（候选必须记录在设施契约卡的"扩展点"字段）。

**优先级**：先做差异分类法（2.1），直接同构合并发生 ≥2 次后，共同形态自然浮现时再做超概念泛化。**禁止**自上而下预先设计抽象框架等待实例入驻。

### 2.3 终审规则

以下两道终审同时适用，任一不通过即**禁止**抽象：

**终审一（体内分支检验）**：将 A、B 合成一个函数后，函数体内若仍需 `if (mode == A) ... else if (mode == B)` 之类分支来保留二者差异，则两者不是同一骨架，**必须**保留各自实现，最多抽取二者共同调用的纯函数。

**终审二（缺陷实现检验）**：若同构差异中一侧是缺陷实现（如 `geo_dynamic` 的 `visited % 256` 碰撞缺陷），另一侧是正确实现，则**必须**以正确语义为唯一语义抽象合并，并**必须**添加回归测试钉住"行为被修正"。

---

## 3. 抽象方向与豁免

### 3.1 策略上提

- 结构算法（怎么走）**必须**上提到公共层。
- 数据（走什么）**必须**留在原模块，通过参数 / 回调暴露。
- 上提结果**必须**是无状态、无领域假设的纯函数 / 纯结构。

### 3.2 数据 / 语义下放

数据布局与语义本身同样是抽象对象，按类别收敛：

| 重复形态 | 统一设施 |
|----------|----------|
| 增长模式 | `lv_ensure_capacity` |
| 语义常量 | `config.h` 常量族 |
| 按键查找表 | `lvHashtable` |
| 数值判定语义 | `lv_is_zero` / `lv_is_integer_double` |
| 计时 / 时间 | `lv_get_time_us` / `lv_clock_elapsed_sec` |

### 3.3 领域豁免区

**定义**：合并后改变任一侧语义输出的代码区域。

**判定问题**：合并后是否会改变任一侧的输入输出语义？会 → 豁免。

**规则**：
- 豁免区**必须**在代码处以 `/* exempt: <语义说明> */` 注释显式标注。
- 豁免点**必须**登记到第 9 章决策登记，否则视为漏网。
- 典型豁免：`algebraic_number_poly.c` 的 mpq 精确判别式 ≠ double 二次根求解（代数数内核精确性语义）；`lv_number.c` 的 `(int64_t)` 截断版本 ≠ 浮点比较。
- 泛化版豁免规则（语义契约单一性）：超概念 C 的所有特例**必须**共享同一输入输出语义契约。禁止把不同语义的类型（如 mpq 精确数与 double 近似数）泛化进同一 C。

---

## 4. 设施形态

### 4.1 统一形态

```
抽象对象 = (函数签名, 策略常量, 回调钩子)
```

| 层 | 职责 | 禁止 |
|----|------|------|
| 签名 | 表达"做什么" | 依赖具体领域类型 |
| 常量 | 编译期参数（epsilon、容量初值、比较模式） | 魔术数字内嵌 |
| 回调 | 领域特定比较 / 访问 / 判定 | 策略逻辑下放到回调 |

### 4.2 命名契约

**命名即契约**：设施名是语义承诺的载体，改名成本最高，命名**必须**一次性定稿。

- 使用语义动词 / 能力词，**禁止**使用机制词（✅ `lv_ensure_capacity`；❌ `lv_double_realloc`——增长策略改变后名字即撒谎）。
- 类型特化**必须**用后缀表达：`lvHashtable_i64`、`lv_str_trim`。
- 同一族谓词**必须**统一动词：`lv_is_zero` / `lv_is_equal` / `lv_is_integer_double`。
- 命名不合格的判定信号：调用点必须阅读实现才能理解其行为。

### 4.3 C 形态决策

| 条件 | 形态 | 实例 |
|------|------|------|
| 语义单一、特例多、类型各异 | `void* + size + 回调`（qsort 风格） | `lv_topo_run` |
| 语义单一、类型固定 | 类型特化 + 后缀命名 | `lvHashtable_i64`、`lv_str_*` |
| 任何情况 | **禁止**宏泛型（符号调试与可读性代价过高） | 无 |

### 4.4 契约卡

每个新设施**必须**在头文件注释中携带契约卡，五个字段缺一不可：

| 字段 | 内容 |
|------|------|
| 语义契约 | 一句"做什么 + 不做什么" |
| 前置条件 | NULL / 长度 / 对齐约束 |
| 失败 / 截断语义 | 溢出、截断、容量不足时的行为 |
| 边界行为 | 空输入、满容量、0 长度 |
| 扩展点 | 编译期常量 / 回调钩子清单（含已命名的第三候选，如有） |

**已有实例**：`lv_strlcpy` 必须写明"保证 NUL 终止"（与 `strncpy` 的关键差异），否则后续调用点会按旧假设使用而静默引入 bug。

---

## 5. 公共 API 边界

- 抽象设施进入 `core/include/lv/` 公共头，等同于对外 API 承诺，签名**必须**视为终态。
- 能放内部**必须**放内部（模块内部头 / `static`），进公共头前**必须**确认命名、签名、契约卡三者定稿。
- 违反信号：需要变更已进公共头的设施签名——**禁止**静默变更，必须走版本记录（`VERSION_LOG.md`）。
- 已有实例：`stream_timestamp_ms` 对外保留 `long` 签名；`lv_number.c` 有意保留 `(int64_t)` 截断版本。

---

## 6. 负面清单（禁止抽象的情形）

出现以下任一情形，**禁止**抽象：

1. **稳定前的代码**：接口仍处于频繁演进期。等语义稳定后再收敛。
2. **外部契约强耦合处**：协议格式、文件格式、SMT 输出、互操作字符串。这些是格式契约而非代码，抽象会丢失精确对齐。
3. **领域内核**：`algebraic_number_poly` 的 mpq 核心、solver 精确性核心（豁免区）。
4. **调用点迁就**：为适配新设施而修改调用点的语义。这是最强危险信号，出现即停止——说明并非同构。
5. **无候选的孤例**：单次使用且无命名的潜在第二调用点。

---

## 7. 失败信号与回滚

**设施可回滚**：拆掉不合适的设施不是失败记录，是正常治理行为。

四个失败信号，出现任一**必须**评估回滚：

1. 设施体内长出 mode 分支（区分特例）——拼凑而非同构。
2. 调用点开始绕过设施手写旧模式（治理黑名单命中数回升）。
3. 新增调用点必须"迁就设施修改语义"才能进入。
4. 设施参数随特例数持续增加。

**回滚规则**：回滚后**必须**在决策登记中标注"回滚 + 原因"。拆得干净比硬撑重要。

---

## 8. 测试要求

抽象设施的测试**必须**覆盖三层：

| 层 | 内容 | 基线要求 |
|----|------|----------|
| 等价性测试 | 新旧输出对比 / 对拍，证明行为等价 | **必须**进 ctest 基线 |
| 边界测试 | 空输入、满容量、截断点、0 长度 | **必须**进 ctest 基线 |
| 性质测试 | 增长幂次不变量、NUL 终止不变量、键不存在语义 | 至少一条进 ctest 基线 |

**语义锚点**：涉及该设施的 8 个示例（`example_*`）**必须**全部运行通过，作为跨设施语义锚点。

---

## 9. 决策登记

每个抽象决策**必须**在项目记忆中登记一行，格式：

```
设施 / 判据（A|B|C|泛化）/ 调用点数 / 豁免理由（如有）/ 测试名
```

- 治理黑名单命中的旧模式**必须**查登记表，判定为"已收敛"、"豁免中"或"漏网"。
- 豁免点必须有登记，否则默认按漏网处理。
- 登记是第 3.3 节豁免标注的机器可读版本，二者必须一致。

**批次 R（2026-08-17，C-⑱ 抽象化收敛）**：
- `lv_PI` / `lv_TWO_PI` / `lv_HALF_PI` / `lv_QUARTER_PI`（config.h，判据 C）——M_PI 平台宏收敛：14 文件 52 处迁移（含 M_PI_2 → lv_HALF_PI）；double 逐位等价已验证（3.141592653589793116）；豁免 2 处（geo_visual_complete.c 输出给外部 Cairo 解释器的脚本文本，负面清单 #2 外部契约，已标注 exempt）；测试：interval_arith_test 等 8 项 + 全量 174/174。
- `set_error_msg` varg 化（lv_impl_upper_orchestrator.c，判据 A）——阶段错误消息格式化组装 14 处裸 snprintf 吸收至 vsnprintf 封装；缓冲区大小集中管理；测试：orchestrator 相关 + 全量。
- `lv_snprintf`（lv_utils.h，判据 L，批次 U 设施复用）——prop_verifier_api.c 5 处错误消息组装迁移；行为等价（NUL 终止保证 + 返回值语义一致）；测试：prop_verifier 族 11 项 + 全量。
- 黑名单新增：裸 `M_PI`（应引 lv_PI，含 lv_TWO_PI/lv_HALF_PI/lv_QUARTER_PI 变体）；错误字段裸 `snprintf(xxx.error_msg/error_message, sizeof, ...)`（应走 varg 封装或 lv_snprintf）。

---

## 10. 迁移顺序

按控制流密度排序，**必须**按此顺序推进，不得跳级：

| 阶段 | 对象 | 侧重判据 |
|------|------|----------|
| 1 | 控制流密集：solver 多分支、rewrite 遍历、判定器 | A |
| 2 | 字符串 / 解析：strtok、strncpy、手写前缀匹配 | A |
| 3 | 数值 / 数学：epsilon、判定谓词、归一化阈值 | C |
| 4 | 容器 / 成长：realloc 倍增、线性查找表 | B |
| 5 | 元数据 / 生命周期家族：枚举↔字符串表、分发表、逐字段析构、后端守卫、维度展开 | D / E / F / G / H |
| 6 | 样板收敛：数字提取、字符串安全复制、错误结果（批次 N）、snprintf 定长复制（批次 U） | I / J / K / L |

**每阶段执行序列**：
1. 列清单：grep 计数、调用点清单、语义验证方法。
2. 写设施：新函数 + 单测（先单测后切换）。
3. 逐个切换调用点：单次编辑，逐个确认，**禁止**批量盲改。
4. 回归：ninja 936/936 + ctest 169/169 + 8 个示例。
5. 治理：将旧模式的 grep 模式加入黑名单。

---

## 11. 治理机制

### 11.1 黑名单（新代码命中即评审）

| 模式 | 含义 |
|------|------|
| 裸 `realloc(` | 未走 `lv_ensure_capacity` |
| 裸 `fabs(` | 未走数值谓词 / `approx_eq` |
| `strtok` | 未走 `lv_strtok_r` |
| `1e-` 字面量 | 未走具名 epsilon 常量 |
| 手写 `clock_gettime` | 未走 `lv_get_time_us` |
| 裸 `M_PI` / `M_PI_2` | 未走 `lv_PI` 家族（config.h 权威源；外部契约字符串豁免） |
| 错误字段裸 `snprintf(xxx.error_msg/error_message, sizeof, ...)` | 未走字段级 varg 封装或 `lv_snprintf` |

### 11.2 白名单 / 豁免审计

- 豁免标注 `/* exempt: <语义说明> */` 可被 grep 审计。
- 审计周期：每批次收尾统一检查。

### 11.3 新设施准入

**必须**满足三条，缺一**禁止**入库：

1. 真实调用点 ≥ 2（泛化 ≥ 3，或 2 + 1 命名候选）。
2. 至少一条测试进入 ctest 基线。
3. 全库旧模式 grep 计数归零（或进入显式白名单）。

**已准入设施（批次 L P2，2026-08-10）**：`lv_tree_release_recursive`（lv_graph_traversal.h，回调驱动后序递归释放，判据 A）——第一批真实调用点 3（lvTreeNode / lvProofTreeNode（lvDArray 适配）/ BacktrackNode），测试经 test_proof_trace / test_orchestrator destroy 路径覆盖。第二批候选 3 例（proof_trace_tree.c / expr_canonical.c / gappa_dsl.c）形态略异，已登记待后续，迁移时复用本设施。

**已准入设施（批次 N，2026-08-11）**：`lv_parse_int_before`（lv_parse_utils.h，判据 I，4 调用点，test_meta_verify）；`lv_strlcpy` / `lv_strlcat`（lv_utils.h，判据 J，全库 336 调用点，strncpy 归零）；`lv_RESULT_FAIL`（lv_error.h，判据 K，34 调用点，test_error_handling）；`lv_registry_remove_prefix`（lv_registry.h，判据 A 变体，4 调用点，test_registry）；`lv_constraint_has_participants`（lv_constraint_guard.h，判据 H 泛化，25+ 调用点，test_smt_backend 等）。

---

## 12. 合规检查清单

提交任何抽象改动前，逐项自检：

- [ ] 判据类型已判定（A / B / C / D / E / F / G / H / I / J / K / L / 泛化）
- [ ] 粒度门槛三条全部满足
- [ ] 同构判定通过（差异分类法或超概念检验 + 两道终审）
- [ ] 不触碰豁免区（或已显式标注 `/* exempt */` + 登记）
- [ ] 命名符合 4.2（语义词，非机制词）
- [ ] 契约卡五字段齐全
- [ ] 公共 API 边界已确认（能内部就内部）
- [ ] 负面清单五条全部不命中
- [ ] 测试三层齐全，进 ctest 基线
- [ ] 决策登记已写入项目记忆
- [ ] 旧模式 grep 归零（或白名单）
- [ ] 回归通过：931/931 + 170/170 + 8 个示例

---

*本规范是治理文档，与性能、形式化、技术栈专章正交。判定争议以第 9 章决策登记为裁决依据。*
