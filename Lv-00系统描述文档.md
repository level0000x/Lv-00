# Lv-00 几何元语言系统 v3.0.1

## 概述

Lv-00 是一门以几何为唯一载体的双模数学元语言。几何体本身是计算的执行者、数据的承载者、证明的见证者。

**核心特性：**
- 几何即符号：点、线、区域本身就是程序的实体
- 数形不二：数值只能是几何量
- 构造即证明：构造是否构成证明取决于合一检查
- 公理中立：内核不内建距离、角度概念
- 可演进：公理系统可升级，定理可固化为新规则

## 模块结构

### 核心模块

| 模块 | 文件 | 功能 |
|------|------|------|
| 符号坐标系统 | `symbolic_coord.h/c` | 有理数、代数数、二次根式、超越常数 |
| 约束图核心 | `constraint_graph.h/c` | 点、线段、区域、端口、函数块节点及约束 |
| 图规范化遍引擎 | `normalization.h/c` | 点合并、线段合并、区域合并、幂等性保证 |
| 符号代数求解器 | `solver.h/c` | 代数方程转化、Gröbner基求解、多解处理 |
| 图重写引擎 | `rewrite.h/c` | 匹配算法、替换操作、循环检测 |
| 合一检查 | `unify.h/c` | 端口匹配、约束匹配、坐标判等 |

### 公理系统

| 模块 | 文件 | 功能 |
|------|------|------|
| 公理系统包 | `axiom_pkg.h/c` | 模板定义、不可构造问题列表、依赖验证 |
| 模块系统 | `module.h/c` | 模块加载、依赖管理、版本控制 |

### 高级功能

| 模块 | 文件 | 功能 |
|------|------|------|
| 函数块系统 | `func_block.h/c` | 打包操作、确定性检查、多解选择器、组合子 |
| 函数块选择器 | `func_block_selector.h/c` | 多解分支选择器、条件分支 |
| 函数块组合器 | `func_block_compose.h/c` | 函数复合、乘积、兼容性检查 |
| 类型系统 | `type_system.h/c` | 宇宙层级、类型等价检查、类型推断 |
| 命题与证明系统 | `proof.h/c` | 命题模式、合一检查、证明导航器、爆炸原理 |
| 命题验证器 | `prop_verifier.h/c` | 不可构造性验证、信任颜色判定 |
| 公式系统 | `formula_parser.h/c`, `formula_renderer.h/c`, `formula_converter.h/c` | 公式解析、公式渲染、公式转换 |
| 高维模块 | `high_dim.h/c` | 高维几何体投影、保真度分析、投影预设 |
| 交互模块 | `interop.h/c` | 外部格式导入导出（GeoJSON等） |
| 递归与条件 | `recursion.h/c` | 测度系统、选择器块、递归深度监控 |
| 流处理 | `stream.h/c` | 惰性求值流、无限序列 |

### 引擎与调试

| 模块 | 文件 | 功能 |
|------|------|------|
| 主引擎 | `engine.h/c` | 统一入口、模块管理、函数块操作 |
| 调试工具 | `debug.h/c` | 日志系统、一致性断言、性能计数器 |
| 工具库 | `lv00_utils.h/c` | 安全字符串操作、通用辅助函数 |
| 词法分析器共享 | `lexer_shared.h/c` | 共享词法分析基础设施 |
| 错误码系统 | `error_codes.h/c` | 统一错误码定义与转换 |
| 多项式运算 | `mpz_poly.h/c` | 基于GMP的多项式运算 |

## 数据结构

### 符号坐标类型

```c
typedef enum {
    RATIONAL,        // 有理数 (使用GMP mpq_t)
    ALGEBRAIC,       // 代数数 (极小多项式 + 隔离区间)
    QUADRATIC,       // 二次根式 (a + b√n)
    TRANSCENDENTAL   // 超越常数 (π, e)
} SymbolicCoordType;
```

### 节点类型

```c
typedef enum {
    GEOM_POINT,          // 点
    GEOM_LINE_SEGMENT,   // 线段
    GEOM_REGION,         // 区域
    GEOM_PORT,           // 端口
    GEOM_FUNCTION_BLOCK  // 函数块
} GeomNodeType;
```

### 约束类型

```c
typedef enum {
    CONSTRAINT_INCIDENCE,    // 关联
    CONSTRAINT_BETWEENNESS,  // 之间
    CONSTRAINT_INTERSECTION, // 相交
    CONSTRAINT_CONTAINMENT,  // 包含
    CONSTRAINT_CONNECTION    // 连接
} ConstraintType;
```

### 信任颜色

```c
typedef enum {
    TRUST_GREEN,              // 全构造
    TRUST_BLUE_UNEXPLORED,    // 未探索
    TRUST_BLUE_RESOURCE,      // 资源受限
    TRUST_BLUE_OUT_OF_RANGE,  // 超出范围
    TRUST_GREEN_VERIFIED,     // 已证不可构造
    TRUST_YELLOW,             // 条件性不可构造
    TRUST_ORANGE_ORACLE,      // 非构造性oracle
    TRUST_ORANGE_EX_FALSO,    // 爆炸原理
    TRUST_AMBER,              // 数值假设
    TRUST_DARK_ORANGE         // 非构造性+数值假设
} TrustColor;
```

## 主要API

### 引擎初始化

```c
LV00Engine *engine = engine_create();
engine_load_axiom_package(engine, "euclidean.lvz");
```

### 创建几何体

```c
// 创建点（使用符号坐标）
SymbolicCoord *cx = symbolic_coord_create_rational(0, 1);
SymbolicCoord *cy = symbolic_coord_create_rational(0, 1);
SymbolicCoord *coords[] = {cx, cy};
AddNodeResult result = graph_add_point(graph, coords, 2);
int point_id = graph->next_node_id - 1;

// 创建线段
AddNodeResult result2 = graph_add_line_segment(graph, point1_id, point2_id);
int segment_id = graph->next_node_id - 1;

// 创建区域
int segments[] = {seg1_id, seg2_id, seg3_id};
AddNodeResult result3 = graph_add_region(graph, segments, 3);
```

### 添加约束

```c
int participants[] = {point_id, line_id};
graph_add_constraint(graph, CONSTRAINT_INCIDENCE, participants, 2);
```

### 打包函数块

```c
int internal_nodes[] = {node1_id, node2_id};
int input_ports[] = {port1_id};
int output_ports[] = {port2_id};

FuncBlock *fb;
PackResult result = func_block_pack(
    graph,
    internal_nodes, 2,
    input_ports, 1,
    output_ports, 1,
    NULL, 0,  // 跨边界约束处理
    &fb
);
```

### 合一检查

```c
Proposition *prop = proposition_create(1, PROPOSITION_ATOMIC);
proposition_set_pattern(prop, pattern_graph);

UnifyResult result = proof_unify(construction_graph, prop, true);
if (result == UNIFY_OK) {
    printf("命题得证！\n");
}
```

## 编译说明

### 依赖

- C11 编译器
- GMP 库 (GNU Multiple Precision Arithmetic Library)

### 编译步骤

```bash
mkdir build && cd build
cmake ..
make
```

### 运行测试

```bash
./test_basic
# 综合测试现已移至 tests/manual/ 目录
./tests/manual/test_comprehensive
```

## 设计原则

### 公理中立

内核只提供关联几何引擎（点、线、区域的关联、之间、相交、包含、连接关系），不内建距离、角度概念。欧氏几何、双曲几何、集合论、类型论全都是可加载的"公理系统包"。

### 信任边界

- **第一可信基**：C内核（关联几何引擎、符号坐标系统、求解器、重写引擎）
- **第二可信基**：核心公理包的安全重写规则集（需外部汇合性证明）

### 自举路线

1. C实现内核
2. 公理系统编辑器
3. 微自举A：几何可表达性验证
4. 微自举B：几何证明能力验证
5. 可行性原型：λ-演算几何表示
6. 首次自举：命题逻辑验证器

## 文件结构

```
Lv-00/
├── include/lv00/          # 头文件
│   ├── lv00.h             # 主头文件
│   ├── lv00_utils.h       # 工具库
│   ├── symbolic_coord.h   # 符号坐标
│   ├── constraint_graph.h # 约束图
│   ├── normalization.h    # 规范化
│   ├── solver.h           # 求解器
│   ├── rewrite.h          # 重写引擎
│   ├── unify.h            # 合一检查
│   ├── axiom_pkg.h        # 公理包
│   ├── module.h           # 模块系统
│   ├── func_block.h       # 函数块
│   ├── func_block_selector.h  # 函数块选择器
│   ├── func_block_compose.h   # 函数块组合器
│   ├── type_system.h      # 类型系统
│   ├── proof.h            # 证明系统
│   ├── prop_verifier.h    # 命题验证器
│   ├── formula_parser.h   # 公式解析器
│   ├── formula_renderer.h # 公式渲染器
│   ├── formula_converter.h # 公式转换器
│   ├── recursion.h        # 递归与条件
│   ├── stream.h           # 流处理
│   ├── high_dim.h         # 高维模块
│   ├── interop.h          # 交互模块
│   ├── lexer_shared.h     # 词法分析器共享
│   ├── mpz_poly.h         # 多项式运算
│   ├── error_codes.h      # 错误码系统
│   ├── engine.h           # 主引擎
│   └── debug.h            # 调试工具
├── src/                   # 源文件
├── tests/                 # 测试文件
│   └── manual/            # 手动综合测试
├── examples/              # 示例文件
├── 设计.txt               # 功能规格说明书
├── 规划.txt               # 设计规划文档
└── CMakeLists.txt         # 构建配置
```

## 版本历史

- **v3.0.1** (当前版本)
  - 修复文档：完善文件结构，添加所有缺失模块头文件引用
  - 修复文档：API示例中graph_add_point使用正确的SymbolicCoord参数
  - 修复文档：测试命令路径更新（test_comprehensive移至tests/manual/）
  - 修复文档：许可证从"待定"更新为MIT
  - 修复示例：triangle_construction.c 使用exact quadratic坐标代替有理数近似
  - 修复示例：circle_intersection.c 圆的语义建模与相交约束修正
  - 修复示例：high_dim_demo.c 安全字符串操作和多投影视图资源管理
  - 修复示例：function_composition.c NULL检查和类型系统错误处理
  - 代码质量：统一错误码定义，词法分析器代码去重
  - 代码质量：strncpy替换为lv00_strlcpy，memcpy替换为memmove

- **v3.0.0**
  - 完整实现18个核心模块
  - 函数块系统：打包、例化、确定性检查
  - 类型系统：宇宙层级、类型等价检查
  - 命题与证明系统：合一检查、证明导航器
  - 递归与条件：测度系统、选择器块

## 许可证

MIT

## 参考文献

- 设计文档：`设计.txt`
- 规划文档：`规划.txt`
