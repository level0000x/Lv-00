# LV-00 Sample Plugin Configuration

[general]
name = sample_plugin
version = 1.0.0
author = Lv-00 Project
description = A sample plugin demonstrating the LV-00 plugin system
enabled = true

[settings]
log_level = info
max_iterations = 1000
timeout = 30.0

[features]
geometry_operations = true
validation = true
export_formats = json, xml, svg
