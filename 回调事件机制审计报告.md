# Lv-00 事件 / 回调 / 通知标准现状审计报告

> 审计目标：找出「一个需求多种实现」的回调/通知重复点，为全项目统一化提供输入。
> 审计范围：core（C 引擎）事件/回调/通知 + Python `stream_bridge` 桥接层。
> 方法：`lv_callback_list.h` 蓝本阅读 + grep `core/src` 全部回调注册/事件分发实现 + 逐机制归类。

---

## 〇、一句话结论

**回调「注册 + 多订阅者分发」这个需求，在 C 侧已收敛到唯一基座 `lv_callback_list`**（StreamContext、lvEventBus、模块 setter 注册表三处均已委托它实现）；
但 **C 侧仍残留一套手写广播（插件系统 `lv_plugin_broadcast_event`），Python 桥接侧另有一套手写订阅表（`stream_bridge._event_handlers`）**；再加上**数值演化/事件检测域**的一套固定事件表（`geo_event_detect`）与**单槽回调**（`panic/destructor/log 回调`），构成同一语义的多实现集合。

**核心澄清**：StreamContext 与 lvEventBus **不是两套并行事件机制**，而是**两个并行的「事件域」，共享同一个 `lv_callback_list` 基座**。真正没有收敛到基座的是插件系统广播与 Python 订阅表。

---

## 一、回调 / 事件机制清单表

| # | 机制 | 文件（权威位置） | 注册 API | 分发 API | 交互模型 | 事件域 |
|---|---|---|---|---|---|---|
| M1 | **泛型回调列表（基座）** | `core/include/lv/lv_callback_list.h`<br>`core/src/layer2_resource/lv_callback_list.c` | `lv_callback_list_add`（返回自增 ID≥1）/`remove_by_id`/`remove_by_fn`/`set_filter`/`get_filter`/`count` | `lv_callback_list_dispatch(list, arg, filter_fn, invoke_fn)` | 回调列表（push） | **通用基座**（无业务语义；filter 由调用方定义，invoke 负责泛型回调转回真实签名） |
| M2 | **StreamContext 引擎事件** | `core/include/lv/stream.h`<br>`core/src/layer2_resource/stream/{stream_context,stream_buffer,stream}.c` | `stream_register_callback` / `stream_register_callback_ex` / `stream_unregister_callback(_by_id)` / `stream_set_callback_filter` / `stream_get_callback_filter` | `stream_emit` → `stream_dispatch` → `lv_callback_list_dispatch`（`stream_cb_filter`+`stream_cb_invoke`） | 回调列表（push）+ 缓冲/节流/惰性 + 异步队列 + **惰性拉取（poll）** | **引擎事件域**（启动/归一化/重写/求解/证明/函数块/预设/冲突/错误等，48 种 `STREAM_EVENT_*`，filter=事件类型位掩码） |
| M3 | **lvEventBus 运行时事件总线** | `core/include/lv/lv_event_bus.h`<br>`core/src/layer2_resource/lv_event_bus.c` | `lv_event_subscribe(type, cb, user_data)` / `lv_event_unsubscribe(id)` | `lv_event_emit(bus, type, data)` → `lv_callback_list_dispatch`（`ev_bus_filter`+`ev_bus_invoke`） | 回调列表（push）+ 可桥接 Stream | **运行时监控事件域**（`RM_EventType`，filter=整型事件类型，-1=监听所有；经 `STREAM_EVENT_BUS_EVENT` 投射到 Stream） |
| M4 | **模块流上下文 setter 注册表** | `core/src/layer4_reasoning/stream/stream_context_util.c` | `stream_context_register_setter(setter)`（去重） | `stream_context_dispatch_all(ctx)` / `stream_context_clear_all()` → `lv_callback_list_dispatch` | 回调列表（一次性同步分发） | **组件注入/同步机制**（非事件域；把 `engine->stream_ctx` 分发给各模块的 `xxx_stream_ctx` TLS 指针） |
| M5 | **插件系统事件广播** | `core/src/layer5_output/plugin_system_event.c`<br>`core/include/lv/plugin_system.h` | 插件携带 `plugin->on_event` 函数指针；`lv_plugin_set_event_handler(system, handler)` 设单个 `system->event_handler` | `lv_plugin_send_event`（单插件）/ `lv_plugin_broadcast_event`（**手写 for 循环**遍历 `system->plugins[i]->on_event`） | 观察者式单槽 + 手写遍历广播 | **插件事件域**（`lvPluginEvent`；`lv_event_bus.h` 注释明确标注「仍为手写遍历广播，未迁移」） |
| M6 | **日志回调（单槽）** | `core/src/layer2_resource/runtime_monitor.c:217` | `lv_log_set_callback(lvLogCallback, user_data)` | （日志写入时调用单回调） | 单槽回调 | **日志域**（单订阅者） |
| M7 | **紧急保存处理（单槽）** | `core/src/layer2_resource/debug_emergency.c:37` | `debug_set_emergency_handler(handler)` | （信号处理程序调用单回调） | 单槽回调 | **调试/崩溃钩子域**（单订阅者） |
| M8 | **引用计数析构回调（单槽/对象）** | `core/include/lv/debug.h:518`（`lvRefCounted.destructor`） | 对象创建时设置 `destructor` | `ref_count_dec` 归零时调用 | 单槽回调（每对象） | **资源生命周期域**（每对象一个） |
| M9 | **几何演化回调（单槽/实例）** | `core/include/lv/geom_evol.h:199-203` | `evol->rhs_func` / `evol->post_step` / `evol->root_func`（结构体赋值） | `geoevol_step_once` 中调用 | 单槽函数指针（每演化实例） | **数值演化域**（RHS / 步后处理 / 根事件函数） |
| M10 | **事件检测注册表（固定表）** | `core/include/lv/geo_event_detect.h`<br>`core/src/layer3_geometry/**` | `geo_event_register(detector, event_id, type, func, direction, terminal, callback)` | `geo_event_detect(...)`（逐事件符号检测 + 求根） | 固定容量表（128）+ 注册/检测 | **数值事件检测域**（`lvEventType`：交点/碰撞/边界/共面等；每事件自带 func + callback + direction/terminal） |
| M11 | **Python 桥接订阅表** | `module/python/lv/stream_bridge.py:72,260-275` | `add_event_handler(handler)`（append）/ `remove_event_handler(handler)` | `_handle_event` → `for handler in self._event_handlers: handler(event_dict)` | **手写 Python 回调列表**（push） | **Python 消费域**（在单个 C `stream_register_callback_ex` 之上再做一层多订阅分发到 Python 函数） |
| M12 | **IO/UI 块（数据流，非回调）** | `core/include/lv/io_blocks.h`、`core/src/layer6_visual/io/*_block.c` | 无回调注册；`lv_file_block_create`/`lv_network_block_create`/`lv_ui_event_block_create` | `lv_file_block_read/write`、`lv_network_block_send/receive` | 端口数据流（pull/push 数据，非订阅者分发） | **IO/UI 数据流域**（文件/网络/UI 事件端口，无「注册回调+分发」语义） |

补充：`core/src` 下另存在大量 *静态类型分发表*（`*_handlers[]` 按枚举下标索引，如 `func_block.c:s_boundary_action_handlers`、`proof_strategy.c:kStrategyDispatch`、`type_check.c:s_*_handlers`、`interop_export_*` 等）。这些是 **VTable/表驱动分发**，接收者是「类型」而非「可动态注册的订阅者」，与 M1–M5 的「订阅者回调注册」不是同一需求，**不在本次统一化范围内**（仅作背景记录）。

---

## 二、重复回调机制清单（同一需求的多个实现）

### 2.1 「注册多订阅者 + 分发」需求的实现集合

| 需求语义 | 实现 1 | 实现 2 | 实现 3 | 现状 | 判定 |
|---|---|---|---|---|---|
| **"注册一个订阅者，事件来时通知它"** | `lv_callback_list`（M1，权威基座） | StreamContext（M2）/ lvEventBus（M3）/ setter 注册表（M4）——均**委托** M1 | 插件系统 `on_event` + `broadcast`（M5，**手写**，未委托） | 三处已收敛到基座；插件系统未收敛 | **一需求多实现**（应统一到基座） |
| **Python 侧"注册一个处理器，收到 C 事件时调用它"** | C 侧 `lv_callback_list`（M1/M2） | Python `stream_bridge._event_handlers`（M11，**手写 list**） | — | Python 在单个 C 回调之上又建了自己的订阅表 | **一需求多实现**（跨语言边界，需判定豁免或收敛） |
| **"注册一个事件检测函数，命中时回调"** | `geo_event_detect`（M10，固定表） | （无其他通用实现，但与 M1 的「注册+分发」形态同构） | — | 领域专用，未委托基座 | **结构同构但语义专用**（见 2.3 判定） |
| **"设置一个对象/实例的回调"** | geom_evol 单槽（M9）、refcount destructor（M8）、紧急 handler（M7）、log 回调（M6） | — | — | 均为**单槽**回调（一个槽位一个回调），非多订阅列表 | **不同事件域**（单槽，不构成列表重复） |

### 2.2 明确需要统一的重复点

| # | 重复点 | 位置 | 证据 | 统一方向 |
|---|---|---|---|---|
| **D1** | **插件系统手写广播** | `plugin_system_event.c:68-88`（`lv_plugin_broadcast_event`） | `lv_event_bus.h:61-64` 注释明确：「插件系统的 `lv_plugin_broadcast_event` 仍为手写遍历广播……故未迁移」 | 改用 `lvCallbackList`（或直接让插件订阅 `lvEventBus`），替换 `for (i<plugin_count){on_event}` 手写循环 |
| **D2** | **Python 手写订阅表** | `stream_bridge.py:72,251-255,260-275` | `add_event_handler`=append、`remove`=list.remove、`_handle_event`=for 循环分发，与原语 `lv_callback_list_add/remove/dispatch` 一一对应 | 跨语言边界，**倾向显式豁免**：Python 函数无法存入 C `lvCallbackList`，需一层 Python 注册表；**建议在源码标注 `# 对齐 lv_callback_list 语义` 并登记豁免**，避免重复心智 |

### 2.3 结构同构但语义专用（不强行迁移）

| 候选 | 与基座同构的部分 | 语义差异（禁止合并的理由） | 处理 |
|---|---|---|---|
| **geo_event_detect**（M10） | 有「注册 + 分发」骨架（`geo_event_register` / `geo_event_detect`） | 每个事件条目**自带 event-func + callback + direction + terminal + t_prev/t_span**；分发是「逐事件做符号检测和求根」，不是「把事件广播给订阅者」。这是**数值事件检测器**，不是**订阅者列表** | 保留独立实现；如需复用，仅可复用 `lv_callback_list` 的「动态数组」机制承载 event 表，但业务与基座语义不同，**不建议** |
| **geom_evol**（M9） | 结构体里放回调函数指针 | **单槽**（每实例一个 RHS/step/root），且是求解算法核心路径（负面清单第 3 条：领域内核），接口在快速演进 | 保留；非订阅者列表 |
| **IO/UI 块**（M12） | 无回调注册 | 是**数据流**（端口读写），不是**事件通知** | 不在统一化范围 |

---

## 三、事件分发模式盘点（回调列表 vs 观察者 vs 轮询）

| 分发模式 | 出现位置 | 说明 |
|---|---|---|
| **回调列表（push）** | M1 基座；M2 StreamContext（immediate 模式）；M3 lvEventBus；M4 setter | 事件发生 → 依次调用匹配订阅者。**主流、已统一** |
| **观察者式广播（on_event / event_handler）** | M5 插件系统 | 每个插件持 `on_event` 指针；`broadcast` 手写 for 循环逐个调用。**弱观察者，未统一** |
| **轮询 / 拉取（poll / pull）** | M2 惰性模式（`stream_lazy_next` / `stream_lazy_drain` / `stream_lazy_pending`，`stream_lazy.c:74,92,120`）；M3 运行时事件记录 `lv_event_trace_get_all`（`runtime_monitor.c:1109`） | 事件先入队，消费者**主动拉取**。与回调列表并存于同一 StreamContext（拉取 + 推送两套消费接口） |
| **单槽回调（delegate/hook）** | M6 日志、M7 紧急 handler、M8 destructor、M9 演化回调 | 一个对象/系统一个回调槽，非列表语义 |

**结论**：事件分发确有**三种模式**（回调列表 push / 观察者广播 / 轮询拉取）。其中 push 已收敛到 `lv_callback_list`；观察者广播只剩插件系统一处；轮询拉取是 StreamContext 的合法并行模式（面向消费者主动场景），本身不算重复。

---

## 四、StreamContext 与回调列表的关系（是否两套并行事件机制）

**结论：不是两套并行机制。**

1. **StreamContext 内嵌了 `lvCallbackList`**：`stream_internal.h:70` `struct StreamContext { lvCallbackList callback_list; ... }`；`stream_context.c:25` `lv_callback_list_init(&ctx->callback_list, ...)`；`stream_buffer.c:94` 分发委托 `lv_callback_list_dispatch`。→ StreamContext 的「回调注册/分发」就是基座，不是另起炉灶。
2. **StreamContext 与 lvEventBus 是「两个事件域，共享一个基座」**：
   - 事件载荷不同：`StreamEvent`（结构体，type 枚举 + 字段）vs `(int event_type, void *event_data)`。
   - 过滤语义不同：位掩码 `STREAM_EVENT_MASK` vs 整型 `event_type`（-1=全部）。
   - 两者通过 `STREAM_EVENT_BUS_EVENT` 桥接（`lv_event_bus.c:108-124`），运行时事件可投射到 Stream 流。
3. **真正"平行"的是第三个事件域——插件系统（M5）**：它既不共享基座，也不走事件总线，构成第三套事件语义。

所以，「两套并行事件机制」在**回调注册层**不成立；但在**事件域层**确实存在**引擎事件域（StreamContext）/ 运行时事件域（lvEventBus）/ 插件事件域（plugin）三条平行通道**。前两条已共用基座，第三条未共用。

---

## 五、统一化建议：单一回调基座 + 事件域分层

### 5.1 架构目标

```
                 ┌──────────────── 事件域分层 ────────────────┐
  订阅者注册/分发  │  引擎事件域   运行时事件域   插件事件域(待迁)   │
  ┌────────────┐┌─────────────┐┌──────────────┐┌─────────────┐
  │ lvCallback ││ StreamContext││  lvEventBus   ││  (迁移到     │
  │  _list (M1)││ (M2, 位掩码) ││  (M3, 类型)   ││  基座/总线) │
  └────────────┘└─────────────┘└──────┬───────┘└─────────────┘
        ▲（唯一基座）                    │ STREAM_EVENT_BUS_EVENT
        └─────────────── 委托 ──────────┘ 桥接
```

- 唯一「注册+分发」基座 = `lv_callback_list`（M1）。所有**多订阅者**事件域都应委托它。
- 各事件域 = 独立结构封装**自带载荷模型 / 过滤语义 / 发射策略**，内部持有一个 `lvCallbackList`。
- 单槽回调（日志/崩溃/析构/演化 RHS）= 保持单槽，不与列表基座混为一谈。

### 5.2 分层 / 优先级建议

| 级别 | 项目 | 动作 | 价值 | 备注 |
|---|---|---|---|---|
| **P0** | 插件系统广播收敛（D1） | 把 `lv_plugin_broadcast_event` 的 `for(plugin){on_event}` 手写循环改为：为每个插件维护一个 `lvCallbackList`，或让插件订阅全局 `lvEventBus`（用 `lv_event_subscribe`）；`lv_plugin_set_event_handler` 的单槽保留为「系统级钩子」。 | 高 | 已有基座且 `lv_event_bus.h` 已点名未迁移；消除 C 侧唯一手写广播。**注意**：插件广播需为每个插件设置 `event.source`，与事件总线「所有订阅者共享同一 event_data 指针」语义不同 → 需在迁移时确认 `event.source` 语义是否保留（`lv_event_bus.h:62-64` 已记录此差异）。先补一个「插件事件 → 事件总线」的适配层，再切调用点。 |
| **P1** | Python 订阅表（D2） | 不迁移 C 基座（函数指针跨语言不通），但**在源码显式标注**「对齐 `lv_callback_list` 语义：add=append / remove=remove / dispatch=for 循环」，并在治理登记表记录豁免理由。 | 中 | 跨语言边界，属「合理例外」，只需**显式豁免 + 登记**，避免被误判为漏网。 |
| **P2** | geo_event_detect（M10） | 保持独立（语义专用）；如后续要复用，仅复用 `lv_callback_list` 的动态数组承载事件表即可，不迁业务。 | 低 | 数值事件检测属领域内核（负面清单第 3 条），不建议强行表驱动统一。 |

### 5.3 事件域划分（"一需求多实现" vs "不同事件域"）总结

| 判定 | 条目 |
|---|---|
| **一需求多实现，应统一** | 插件系统广播（D1，C 侧唯一手写）；Python 订阅表（D2，跨语言，建议显式豁免 + 登记） |
| **不同事件域，可共用基座（非重复）** | StreamContext（引擎事件）/ lvEventBus（运行时事件）/ setter 注册表（非事件域，组件同步）——已共用 |
| **不同事件域，单槽回调（非列表重复）** | 日志回调 / 紧急处理 / 引用计数析构 / 演化 RHS-step-root |
| **不同事件域，数据流/领域专用（不在统一范围）** | IO/UI 块（端口数据流）；geo_event_detect（数值事件检测）；各类静态类型分发表 |

### 5.4 提交前合规检查（对照 `code-abstraction-governance`）

- [ ] 唯一基座 `lv_callback_list` 是否已承载全部「多订阅者注册+分发」？（当前 C 侧差插件系统一处）
- [ ] 新事件域是否**内嵌 `lvCallbackList`** 而非手写数组/for 循环？
- [ ] 单槽回调是否维持单槽，未被误做列表？
- [ ] 跨语言（Python）重复是否**显式豁免 + 登记**？
- [ ] 类型分发表（VTable）与订阅者列表是否正确区分，未混淆？

---

## 六、证据索引（关键文件:行号）

- 基座：`core/include/lv/lv_callback_list.h`（条目/过滤/dispatch 契约），`core/src/layer2_resource/lv_callback_list.c`（实现）
- StreamContext 委托：`core/src/layer2_resource/stream/stream_internal.h:70,94`、`stream_context.c:25,109-140`、`stream_buffer.c:94,183`
- lvEventBus 委托：`core/src/layer2_resource/lv_event_bus.c:46,64,105`
- setter 注册表委托：`core/src/layer4_reasoning/stream/stream_context_util.c:62,97,103,117`
- 插件手写广播：`core/src/layer5_output/plugin_system_event.c:68-88`；自述未迁移：`core/include/lv/lv_event_bus.h:61-64`
- 运行时刻单槽回调：`core/src/layer2_resource/runtime_monitor.c:217`（log）、`core/src/layer2_resource/debug_emergency.c:37`、`core/include/lv/debug.h:518`（destructor）
- 事件检测/演化：`core/include/lv/geo_event_detect.h:59-69,86`、`core/include/lv/geom_evol.h:199-203`
- Python 订阅表：`module/python/lv/stream_bridge.py:72,251-255,260-275`
- 惰性拉取（poll）：`core/src/layer2_resource/stream/stream_lazy.c:74,92,120`、`core/src/layer2_resource/runtime_monitor.c:1109`
