# 安全策略

## 支持的版本

| 版本   | 支持状态       |
|--------|----------------|
| v3.3.x | ✅ 活跃支持    |
| v3.2.x | ⚠️ 仅安全修复  |
| < v3.2 | ❌ 不再支持    |

## 报告安全漏洞

**请勿通过公开 Issue 报告安全漏洞。**

如发现安全漏洞，请通过以下渠道私密报告：

1. 发送邮件至项目维护者
2. 描述漏洞的详细步骤、影响范围和潜在危害
3. 我们将在 48 小时内确认收到，并在 7 个工作日内给出修复方案

## 安全设计原则

### 内存安全

- 所有内存分配通过统一封装 `lv00_malloc` / `lv00_free` 进行
- 指针释放后强制置 NULL
- 所有入参进行 NULL 检查
- 栈缓冲区使用 `snprintf` 边界检查
- 大缓冲区（>256B）优先使用堆分配

### 输入验证

- 解析器输入长度限制：1 MiB
- Token 数量限制：100,000
- AST 深度限制：256
- 控制字符过滤和 Unicode 空白标准化
- 递归深度熔断器（三态模型：CLOSED → OPEN → HALF_OPEN）

### 运行时防护

- 五态状态机：10 条合法状态转移
- 五维熔断器：时间 / 深度 / 步数 / 内存 / 错误
- 推理分支快照回滚
- 浮点审计标记（`LV00_TOLERATED_FLOAT`）

### 参考

- 本项目不依赖外部网络通信，无远程代码执行风险
- 本项目不写入用户文件系统，无路径遍历风险
- 密级数据保护不适用于本项目（无用户数据存储）

## 已知限制

- 当前版本不支持多线程并发（计划 v3.5）
- 递归深度上限 4096（可配置）
- GMP 库使用系统默认分配器，不受 LV00 内存封装管理

## 安全审计

我们欢迎独立安全审计。审计前请先阅读项目架构文档 [ARCHITECTURE_v3.3.md](docs/ARCHITECTURE_v3.3.md)。

## 致谢

感谢所有负责任的漏洞报告者。
