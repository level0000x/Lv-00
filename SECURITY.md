# 安全策略

## 支持的版本

| 版本   | 支持状态       |
|--------|----------------|
| v3.3.x | ✅ 活跃支持    |
| v3.2.x | ⚠️ 仅安全修复  |
| < v3.2 | ❌ 不再支持    |

## 报告安全漏洞

**请勿通过公开 Issue 报告安全漏洞。**

如发现安全漏洞，请通过以下渠道私密报告：

1. 发送邮件至项目维护者
2. 描述漏洞的详细步骤、影响范围和潜在危害
3. 我们将在 48 小时内确认收到，并在 7 个工作日内给出修复方案

## 安全设计原则

### 内存安全

- 所有内存分配通过统一封装 `lv_malloc` / `lv_free` 进行
- 指针释放后强制置 NULL
- 所有入参进行 NULL 检查
- 栈缓冲区使用 `snprintf` 边界检查
- 大缓冲区（>256B）优先使用堆分配

### 输入验证

- 解析器输入长度限制：1 MiB
- Token 数量限制：100,000
- AST 深度限制：256
- 控制字符过滤和 Unicode 空白标准化
- 递归深度熔断器（三态模型：CLOSED → OPEN → HALF_OPEN）

### 运行时防护

- 五态状态机：10 条合法状态转移
- 五维熔断器：时间 / 深度 / 步数 / 内存 / 错误
- 推理分支快照回滚
- 浮点审计标记（`lv_TOLERATED_FLOAT`）

### 参考

- 本项目不依赖外部网络通信，无远程代码执行风险
- 本项目不写入用户文件系统，无路径遍历风险
- 密级数据保护不适用于本项目（无用户数据存储）

## Python 层面安全风险

### API 密钥存储安全

Python 后端（`python/lv_server.py`）可能使用第三方 API 密钥（如 DashScope）。

**风险**：密钥硬编码在源码中或存储在不安全的配置文件中。

**缓解措施**：
- 所有 API 密钥必须通过环境变量读取（`os.environ.get("lv_API_KEY")`）
- 禁止将密钥提交到版本控制系统（`.gitignore` 中排除 `.env` 文件）
- 配置文件中的密钥使用加密存储或密钥管理服务
- CI/CD 流水线中使用安全的密钥注入机制

### WebSocket 认证

Python WebSocket 服务端需要防止未授权连接。

**风险**：未认证的 WebSocket 连接可能导致未授权的几何计算资源消耗。

**缓解措施**：
- WebSocket 握手阶段验证 token（已实现）
- Token 具有有效期，过期自动断开
- 限制单 IP 的最大并发连接数
- 连接建立后定期重新验证

### CORS 配置

跨域资源共享（CORS）配置影响浏览器端的安全性。

**风险**：过于宽松的 CORS 配置（如 `Access-Control-Allow-Origin: *`）允许任意网站发起请求。

**缓解措施**：
- 使用动态 CORS 白名单匹配（已实现），仅允许可信域名
- 禁止在生产环境中使用 `*` 通配符
- 对敏感操作（如管理接口）使用更严格的 CORS 策略
- 定期审查白名单中的域名列表

### 硬编码密码风险

**风险**：源码中可能包含硬编码的密码、token 或其他敏感凭证。

**缓解措施**：
- 使用 `git-secrets` 或 `truffleHog` 等工具扫描仓库中的硬编码凭证
- CI 流水线中集成凭证扫描检查
- 所有敏感配置通过环境变量或安全配置服务提供
- 代码审查时重点关注字符串常量中的凭证模式

### 内存中用户数据

**风险**：Python 进程内存中可能临时存储用户的几何构造数据和会话信息。

**缓解措施**：
- 会话数据设置合理的过期时间（TTL）
- 实现会话 LRU 清理机制，防止内存无限增长
- 敏感数据在不再需要时及时清除（置零或删除引用）
- 进程退出时确保所有会话数据被正确释放
- 避免在日志中记录完整的用户输入数据

## 已知限制

- 当前版本不支持多线程并发（计划 v3.5）
- 递归深度上限 4096（可配置）
- GMP 库使用系统默认分配器，不受 lv 内存封装管理

## 安全审计

我们欢迎独立安全审计。审计前请先阅读项目架构文档 [README 系统架构](README.md#系统架构)。

## 致谢

感谢所有负责任的漏洞报告者。
