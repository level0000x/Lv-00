# Lv-00 推理／求解后端标准现状审计报告

**审计范围**：`core/src/layer4_reasoning/`（backends / solver / rewrite / unify / engine / dsl / proof_system / model）
**相关规范**：`doc/docs/14_solver_backends.md`、`04_solver.md`、`05_rewrite.md`、`06_unify.md`、`INFERENCE_STRATEGIES_SPEC.md`
**审计目标**：找出"一个需求、多种推理后端"的重复点，为全项目标准统一化提供输入。

---

## 0. 一句话结论

- **真正被使用的求解后端只有一个：GROEBNER（`solve_algebraic_system` 主流程）**；其余 SMT(Z3/CVC5/Singular)、ATP(Vampire/E/iProver)、BDD、probabilistic、approx-counter 均为**框架/桩**，只定义了接口与注册位。
- **不存在单一、被实际求解路径引用的"统一后端注册表"**。项目内实际并存 **5+ 套**注册机制（`lvBackendPluginRegistry`、`SMTBackendRegistry`、`ATPBackendRegistry`、`EngineScheduler` 后端数组、数值后端注册表、存储后端注册表），而**引擎真实分发走的是 `engine_scheduler.c` 里一张硬编码静态 VTable，绕开了所有注册表**。
- **"判定约束可满足性"这个需求有 ~9 条平行路径**，它们**大多互不调用、平行存在**。
- **rewrite / unify / 证明策略 / 模型计数 / 数值后端属于"不同推理策略"**，应保留分层；只有"同一语义的 SAT/SMT/代数能否求解"属于"一需求多实现"，应统一到注册表收敛。

---

## 1. 推理后端 / 入口清单表

### 1.1 backends/ 目录实际归属（27 个源文件）

| 后端域 | 开始文件 | 说明 |
|---|---|---|
| **GROEBNER** | `backends/groebner_engine.c`, `groebner_engine_core.c`, `groebner_engine_ideal.c`, `groebner_engine_poly.c`, `groebner_engine_ring.c`, `groebner_engine_variety.c`, `groebner_mono.c`, `groebner_parallel.c`, `groebner_poly.c` | 唯一真实可用的求解后端（Buchberger + 理想成员判定） |
| **SMT** | `backends/smt_backend_impl.c`, `smt_backend_impl_external.c`, `smt_backend_impl_groebner.c`, `smt_backend_impl_smtlib2.c`, `smt_bitvector.c`, `smt_theory_combiner.c`, `smt_trigger_engine.c` | SMT-LIB2 编解码 + 理论组合 + E-matching，仅 GROEBNER 分支可用 |
| **BDD / ADD** | `backends/bdd_encoding.c`, `bdd_encoding_add.c`, `bdd_encoding_cnf.c`, `bdd_encoding_encode.c`, `bdd_encoding_sift.c` | 符号化布尔／代数决策图（桩，预留接口） |
| **模型计数** | `backends/approx_counter.c` | 近似 #SAT（ApproxMC 风格，桩） |
| **概率约束** | `backends/probabilistic_constraint.c` | 概率约束处理（桩） |
| **ATP** | `backends/atp_backend.c` | FOL 一阶自动定理证明（桩，框架注册 Vampire/E/iProver） |

> 另有一组**独立的数值计算后端**（非推理后端）：`layer4_reasoning/numeric/backends/`（`cuda_backend.c`、`hip_backend.c`、`singular_backend.c`、`default_host_ops.c`）+ `numeric/numerical_backend.c`，对应 `lvBackendType`（SERIAL/OpenMP/CUDA/HIP/SINGULAR），用于线性代数／数值求解，**与推理后端判可满足无关**，属于不同关切。

### 1.2 求解器/后端抽象接口（四个互不兼容的"求解器"族）

> **关键发现**：不存在一个统一的 `lvSolver` vtable。项目里有 **4 个彼此独立的"求解器"类型**，各有自己的不透明句柄与 API，**没有公共抽象基类或统一 vtable**：

| 接口头文件 | 求解器类型 | 所在实现 | 语义 | 实际负载 |
|---|---|---|---|---|
| `core/include/lv/solver.h` |（无句柄，函数式）`solve_algebraic_system()` | `solver/solver_engine.c`（主入口，8 步流水线）plus `solver.c`、`solver_incremental.c` | 几何约束 → 代数方程 → Gröbner 基求解 | **引擎真实主求解流程** |
| `core/include/lv/solver_core.h` | `lvSolver`（不透明句柄） | `solver/solver_core.c` | **CDCL SAT 求解器**（10 状态机，CaDiCaL 风格） | 仅被 `solver/sat_encoding*.c` SAT 编码管线使用 |
| `core/include/lv/smt_backend.h` | `SMTSolver`（不透明句柄） | `backends/smt_backend_impl.c` + `smt_backend_impl_groebner.c` | SMT-LIB2 → 后端求解 | 仅 GROEBNER 分支实现 |
| `core/include/lv/geo_constraint_solver.h` | `lvSolverSystem` | `layer3_geometry/geo_constraint_solver_*.c` | **Newton 数值求解**（拖拽交互实时求解） | layer3 几何交互层 |

`doc/docs/14_solver_backends.md` 描述的 `solver_core.h → SMTBackend → engine_scheduler` 多层架构，是对"应该长什么样"的规范表述；**实现上逻辑拆成 4 个平行求解器族 + 独立的 AST 抽象**，并没有用统一 vtable 串起来。

### 1.3 各后端/入口是否"注册表化"

| 后端/入口 | 接口 | 是否有注册表 | 是否被真实求解路径引用 |
|---|---|---|---|
| GROEBNER | `solve_algebraic_system()` | 无注册表（函数直接调用） | ✅ 是（引擎主路径、调度器 groebner_solve、func_block_determinism） |
| CDCL SAT（`lvSolver`） | `lv_solver_solve()` / `lv_solver_solve_algebraic()` | 无注册表 | ⚠️ 仅 SAT 编码管线（`sat_encoding_decode.c`） |
| SMT | `smtsolver_solve()` / `smtsolver_check()` | ✅ `SMTBackendRegistry`（`smtsolver_register_backend`, `smtsolver_get_registry`） | ⚠️ 独立 API 消费者；引擎主路径**不走它** |
| ATP | `atp_solver_solve_graph()` / `atp_auto_solve()` | ✅ `ATPBackendRegistry` + `lv_registry`（`atp_register_backend`） | ⚠️ 桩，无真实求解 |
| 引擎调度后端 | `scheduler_solve_with_backend()` | ✅ `EngineScheduler.backends[]` + `scheduler_register_backend` | ✅ **走它，但用静态 VTable** |
| **统一后端插件** | `lv_backend_plugin_register()/find()` | ✅ `lvBackendPluginRegistry`（全局，layer2_resource） | ❌ **仅元数据/适配层，引擎分发不查它** |
| 存储后端 | `lv_storage_register_backend()` | ✅ `lv_registry`（`backend_registry`） | 存储域，无关 |
| 数值后端 | `lv_numerical_backend_register()` | ✅ 数值注册表 | 数值域，无关 |

---

## 2. 同一需求的平行路径清单（"判定约束可否满足/可解"）

**需求**：给定约束图，判定"是否可满足/有无解/是否一致"。全项目至少有 **9 条**平行入口，且大多互不调用：

| # | 入口 | 位置 | 内部实现 | 与主路径关系 |
|---|---|---|---|---|
| 1 | **DSL `check_sat`** | `dsl/dsl_compiler_load.c:402` `handle_check_sat()`（IR_CHECK_SAT, op 27） | `graph_check_compatibility()` (`layer3_geometry/constraint_graph/graph_node_conflict.c:449`) —— **DOF/冗余/冲突的轻量兼容性判定，非真正求解** | 平行、独立（DSL 前端） |
| 2 | **DSL `prove` 预检** | `dsl/dsl_compiler_load.c:383` `handle_prove()` | 同上 `graph_check_compatibility()` | 平行、独立 |
| 3 | **引擎主入口** | `engine/engine_solve.c:363` `engine_rewrite_and_solve()`、`:164` `engine_solve()` | → `run_solver_on_graph()` (`engine_solve.c:82`) → 调度器选中 GROEBNER 走 `scheduler_solve_with_backend()`，否则回退 `solve_algebraic_system()` | **真正主流程** |
| 4 | **引擎调度器** | `engine/engine_scheduler.c:823` `scheduler_solve_with_backend()`，`:754` `groebner_solve()`，`:868` `scheduler_solve()` | `groebner_solve()` 内部再调 `solve_algebraic_system()`；非 GROEBNER 走 `smt_unavailable_solve()`（返回 ERROR） | 与 #3 是同一主流程两条腿 |
| 5 | **代数求解器（直达）** | `solver/solver_engine.c:80` `solve_algebraic_system()` | 8 步流水线：提取方程→超定→out-of-scope→拓扑排序→几何消元→矛盾→Gröbner→结果判定 | 被 #3 #4 #函数块 复用，是真正核心 |
| 6 | **CDCL SAT 核心** | `solver/solver_core.c:1204` `lv_solver_solve()`、`:1473` `lv_solver_solve_algebraic()` | 命题 SAT（10 状态机）；`lv_solver_solve_algebraic` 内部经 `groebner_parallel` 回退代数 | 平行（仅 `sat_encoding_decode.c:60,103` 使用） |
| 7 | **SAT 编码管线** | `solver/sat_encoding*.c`（`sat_encoding_decode.c:60`） | 关系约束 → CNF → 交给 `lvSolver` SAT | 平行、自成体系 |
| 8 | **SMT 后端** | `backends/smt_backend_impl_groebner.c:1267` `smtsolver_solve()` | 仅 GROEBNER 分支真实现（内部调 groebner），Z3/cvc5/Singular 为不可用桩 | 独立 API；引擎主流程不走 |
| 9 | **ATP 后端** | `backends/atp_backend.c:750` `atp_solver_solve_graph()`、`:1127` `atp_auto_solve()` | TPTP→外部 ATP（Vampire/E/iProver），**全部桩** | 独立 API；未接入主流程 |
| 10 | **关系模型有界判定** | `model/relation_model.c:1002` `relation_check_satisfiability()` | RelModel + `SmallScopeConfig` 有界模型判定 | 平行（语义与几何代数路径不同） |
| 11 | **Newton 数值求解** | `layer3_geometry/geo_constraint_solver_newton.c:176` `lv_geo_solver_solve()` | 残差/雅可比迭代数值求解 | 平行（交互拖拽层，与 layer4 代数路径不同） |

**结论**：#1、#2 是两个"轻量 pre-check"（DOF 兼容性），#3 #4 #5 是**真正求解主链**（同一套 Groebner 流程的三个壳），#6 #7 是**命题 SAT 支路**，#8 #9 #10 #11 是**各自独立的平行域**。**"谁调用谁"上：唯一被多模块复用的是 `solve_algebraic_system()`**；其余入口彼此隔离、平行存在。

---

## 3. 推理后端注册机制现状（关键结论）

1. **存在"统一"抽象，但真实路径不用它。**
   `core/include/lv/lv_backend_plugin.h` 明确写着"统一 ATP、SMT、数值计算、调度器和通用注册表五套后端注册机制"，提供 `lvBackendPluginRegistry` + `lv_backend_plugin_*` API，并已由 SMT/ATP 通过 `smt_register_all_plugins()`、`atp_register_all_plugins()` 用 `lv_once` 注册到全局单例。**但引擎真实分发（`engine_scheduler.c:811` 的 `kSchedulerBackendVTables` 静态 VTable）完全不查询这个插件注册表**——它是对实体信息/能力的一个适配层，没有成为求解入口。
2. **注册表林立、语义重叠、互不打通。**
   - `SMTBackendRegistry`（`smt_backend_impl.c`）——SMT 后端
   - `ATPBackendRegistry` + 内部 `lv_registry`（`atp_backend.c`）——ATP 后端
   - `EngineScheduler.backends[]` + `scheduler_register_backend()`（`engine_scheduler.c`）——引擎调度后端
   - `lvBackendPluginRegistry`（`layer2_resource/lv_backend_plugin.c`）——"统一"适配层
   - `numerical_backend`（`lvBackendType`）——数值计算后端
   - `lv_storage` 的 `backend_registry`——存储后端
   它们各自实现注册/查找，**同一后端（如 GROEBNER）在 SMT、调度器、插件、数值四套里各有一份实例**，靠手工枚举表（`LV_SMT_BACKEND_ENTRY`、`kSchedulerBackendVTables`、`s_smt_plugins`、`s_atp_plugins`）维持命名一致。
3. **GROEBNER 是唯一可用，其余为占位。** `doc/docs/14_solver_backends.md` 已如实说明"GROEBNER 为真实可用的求解实现，其余为桩实现或框架定义"。调度器 `smt_unavailable_solve()` 对 Z3/cvc5/Singular 直接返回 `SMT_ERROR_BACKEND_UNAVAILABLE`。
4. **路由规则实际几乎总是选 GROEBNER。** `scheduler_load_preset_rules()` 只装两条：`small-groebner`（var≤50→GROEBNER，priority 20）与 `default-groebner`（无条件→GROEBNER，priority 100）；没有"非线性→SMT""量词→cvc5"等规则被真正启用 → 调度器形同虚设，只会选 GROEBNER。

---

## 4. 统一化建议（推理注册表 + 各策略分层边界）

### 4.1 应"统一/注册表化"——同一需求多实现（收敛点）

判定**"代数/命题约束是否可满足/可解"**这类需求，当前的重复与割裂点：
- `solve_algebraic_system()`（Groebner 主流程）被 3 处壳重复调用：`engine_solve.c`、`engine_scheduler.c:groebner_solve`、`func_block_determinism.c`（×3）。**建议**：以"求解后端"为粒度收敛，`solve_algebraic_system` 作为 GROEBNER 后端唯一的 vtable/函数实现，所有调用方统一经由调度器。
- 调度器分发用**静态 VTable**（`kSchedulerBackendVTables`），而非注册表。**建议**：把调度器分发改走 `lvBackendPluginRegistry`（真正的统一注册表），驱动实现（`groebner_solve`/`smt_unavailable_solve`）注册进 `ops` vtable，删除重复的 `SMTBackendRegistry`/`ATPBackendRegistry` 手工枚举，或以插件包装器收敛到同一事实源。
- GROEBNER 这一后端在三套注册表（调度器 / SMT / 插件）+ 两张静态表（`LV_SMT_BACKEND_ENTRY`、`kSchedulerBackendVTables`）里各有一份。**建议**：新增后端只改一处（如 `LV_SMT_BACKEND_ENTRY`），其余由代码生成/派生（项目已用 `LV_*_ENTRY` 宏做单一事实源，应把命名映射扩到调度器 VTable 与插件描述符）。
- "轻量 vs 精确"两套可满足性判定（`graph_check_compatibility` DOF pre-check vs `solve_algebraic_system` 精确求解）职责不同，**不算重复**，但建议在命名与文档上明确"兼容性预检 ≠ 可满足性判定"。

### 4.2 应"保留分层"——不同推理策略（勿over-合并）

以下属于**语义不同的策略/载体**，不应强行统一到同一注册表：

| 策略 | 位置 | 为何分层保留 |
|---|---|---|
| **重写（rewrite）** | `rewrite/`（`rewrite_with_rules`, `rewrite_strategy_apply`, `rewrite_search_backward`） | 是**对约束图做规范化/化简**的变换策略（`engine_solve` 步骤1、`engine_rewrite_and_solve` 交替），与"求解"是**协作**而非竞争；`INFERENCE_STRATEGIES_SPEC` 也把 Rewrite 列为八大策略之一 |
| **合一（unify）** | `unify/`（`unify_construction_with_proposition*`） | 是**构造图↔命题图匹配验证**（证明检查），属"验证/匹配"而非"求解"；被 `proof` / `simple_proof_check` 使用 |
| **证明策略（proof strategies）** | `proof_system/proof_multi_strategy.c`（12 策略：intro/elim/rewrite/induction/contradiction/case-split/generalize/specialize...） | 是**定理证明的演绎策略调度**，与"后端可满足性求解"是不同推理层级 |
| **模型计数 / 概率 / BDD** | `backends/approx_counter.c`, `probabilistic_constraint.c`, `bdd_encoding*.c` | 是**计数/近似/符号化**能力，与"SAT/代数判定"输出不同（#SAT vs SAT） |
| **数值后端** | `numeric/backends/` + `numerical_backend.c` | 是**线性代数/数值计算**层（SERIAL/CUDA/HIP/SINGULAR），与推理后端类型不同（`lv_backend_plugin.h` 已明确区分 `lvBackendType` vs `lvBackendPluginType`） |
| **Newton 交互求解** | `layer3_geometry/geo_constraint_solver_*.c` | 是**拖拽实时交互**迭代求解，与 layer4 精确代数求解分层 |

### 4.3 建议落地顺序（按收益/风险）

1. **建统一的"推理后端注册表"**：以 `lvBackendPluginRegistry` 为唯一事实源，补上 `ops`（vtable）字段的实际绑定，让 `scheduler_solve_with_backend` 与 `solve` 都走该注册表，删除 `kSchedulerBackendVTables` 与「SMT/ATP 各一份注册表」。
2. **用宏/单一事实源收敛命名**：把 `LV_SMT_BACKEND_ENTRY` 派生范围扩到调度器 VTable 与插件描述符，避免 GROEBNER 在四处手写。
3. **明确入口分层文档**：在 `doc/docs/14_solver_backends.md` 补充"当前引擎真实分发路径（`engine_solve.c:run_solver_on_graph` → 调度器 → `solve_algebraic_system`）"与"4 求解器族各自适用域"，消除规范与实现的落差。
4. **保留策略分层**：rewrite / unify / proof_strategy / numeric / newton 明确归为"不同推理策略"，不纳入统一求解后端注册表。

---

## 附录：关键证据位置

- 后端文件清单：`core/src/layer4_reasoning/backends/*.c`
- 引擎主入口：`engine/engine_solve.c:164` `engine_solve()`、`:363` `engine_rewrite_and_solve()`、`:82` `run_solver_on_graph()`
- 代数求解核心：`solver/solver_engine.c:80` `solve_algebraic_system()`；调度器分发 `engine/engine_scheduler.c:823`、`:754`、`:811`（静态 VTable `kSchedulerBackendVTables`）
- CDCL SAT：`solver/solver_core.c`（`lvSolver`）、`solver/sat_encoding*.c`
- 统一插件注册表：`layer2_resource/lv_backend_plugin.c`、`include/lv/lv_backend_plugin.h`
- SMT/ATP 注册表：`backends/smt_backend_impl.c:529/606`、`backends/atp_backend.c:976/1238`
- DSL 检测入口：`dsl/dsl_compiler_load.c:402`（check_sat）、`:383`（prove）、`:530`（VTable 映射 IR_CHECK_SAT=27）
- 轻量兼容性判定：`layer3_geometry/constraint_graph/graph_node_conflict.c:449` `graph_check_compatibility()`
- 关系模型判定：`model/relation_model.c:1002` `relation_check_satisfiability()`
- 规范：`doc/docs/14_solver_backends.md`、`06_unify.md`、`INFERENCE_STRATEGIES_SPEC.md`
