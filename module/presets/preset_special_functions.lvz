# =============================================================================
# Lv-00 Presets: Special Functions
# =============================================================================
#
# Auto-generated from C preset files.
#
# =============================================================================

lvz 1

presets {

    preset "gamma_function" {
        description "计算Gamma函数 Γ(z) = ∫_0^∞ t^{z-1} e^{-t} dt"
        category "CUSTOM"
        inputs 1 "ANY"
        output "ANY"
        constructive true
        reversible false
    }

    preset "beta_function" {
        description "计算Beta函数 B(a,b) = Γ(a)Γ(b)/Γ(a+b)"
        category "CUSTOM"
        inputs 2 "ANY" "ANY"
        output "ANY"
        constructive true
        reversible false
    }

    preset "incomplete_gamma" {
        description "计算不完全Gamma函数 γ(s,x) 和 Γ(s,x)"
        category "CUSTOM"
        inputs 2 "ANY" "ANY"
        output "ANY"
        constructive true
        reversible false
    }

    preset "error_function" {
        description "计算误差函数 erf(x) = (2/√π) ∫_0^x e^{-t²} dt"
        category "CUSTOM"
        inputs 1 "ANY"
        output "ANY"
        constructive true
        reversible false
    }

    preset "bessel_j" {
        description "计算第一类Bessel函数 J_ν(x)"
        category "CUSTOM"
        inputs 2 "ANY" "ANY"
        output "ANY"
        constructive true
        reversible false
    }

    preset "bessel_y" {
        description "计算第二类Bessel函数 Y_ν(x)（Neumann函数）"
        category "CUSTOM"
        inputs 2 "ANY" "ANY"
        output "ANY"
        constructive true
        reversible false
    }

    preset "bessel_i" {
        description "计算第一类修正Bessel函数 I_ν(x)"
        category "CUSTOM"
        inputs 2 "ANY" "ANY"
        output "ANY"
        constructive true
        reversible false
    }

    preset "hypergeometric_2f1" {
        description "计算Gauss超几何函数 _2F_1(a,b;c;z)"
        category "CUSTOM"
        inputs 4 "ANY" "ANY" "ANY" "ANY"
        output "ANY"
        constructive true
        reversible false
    }

    preset "confluent_hypergeometric" {
        description "计算合流超几何函数 _1F_1(a;b;z)（Kummer函数）"
        category "CUSTOM"
        inputs 3 "ANY" "ANY" "ANY"
        output "ANY"
        constructive true
        reversible false
    }

    preset "legendre_polynomial" {
        description "计算Legendre多项式 P_n(x)"
        category "CUSTOM"
        inputs 2 "ANY" "ANY"
        output "ANY"
        constructive true
        reversible false
    }

    preset "chebyshev_polynomial" {
        description "计算Chebyshev多项式 T_n(x) 和 U_n(x)"
        category "CUSTOM"
        inputs 2 "ANY" "ANY"
        output "ANY"
        constructive true
        reversible false
    }

    preset "hermite_polynomial" {
        description "计算Hermite多项式 H_n(x)"
        category "CUSTOM"
        inputs 2 "ANY" "ANY"
        output "ANY"
        constructive true
        reversible false
    }

    preset "laguerre_polynomial" {
        description "计算Laguerre多项式 L_n(x)"
        category "CUSTOM"
        inputs 2 "ANY" "ANY"
        output "ANY"
        constructive true
        reversible false
    }

    preset "elliptic_integral" {
        description "计算第一类和第二类椭圆积分 F(φ,k), E(φ,k)"
        category "CUSTOM"
        inputs 3 "ANY" "ANY" "ANY"
        output "ANY"
        constructive true
        reversible false
    }

    preset "riemann_zeta" {
        description "计算Riemann Zeta函数 ζ(s) = Σ n^{-s}"
        category "CUSTOM"
        inputs 1 "ANY"
        output "ANY"
        constructive true
        reversible false
    }

    preset "digamma_function" {
        description "计算Digamma函数 ψ(z) = Γ'(z)/Γ(z)"
        category "CUSTOM"
        inputs 1 "ANY"
        output "ANY"
        constructive true
        reversible false
    }

}
