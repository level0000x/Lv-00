[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "lv"
version = "1.1.0"
description = "Lv-00 Geometric Metalanguage — Python Bindings"
requires-python = ">=3.10"
dynamic = ["readme", "authors", "optional-dependencies"]

[tool.ruff]
target-version = "py310"
