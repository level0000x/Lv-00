# Lv-00 抽象方案汇总报告

> 生成日期: 2026-08-04 | 类型: 架构重构规划

---

## 一、已抽象良好的模块（无需重构）

以下模块的抽象设计已被验证为合理，无需进行重构。新后端/新平台扩展时按现有模式操作即可。

| 模块 | 抽象设计 | 扩展成本 |
|------|---------|---------|
| **数值后端** | 三层 vtable（lvVectorOps 17 个函数指针 / lvMatrixOps 10 个 / lvLinearSolverOps 4 个）+ 运行时注册表 BackendEntry（最大 8 个后端），工厂函数通过 `find_vector_ops` / `find_matrix_ops` / `find_linsol_ops` 查表分发 | 新增后端需改 5 处 |
| **线程/并发** | `lv_thread.h` 中全部使用 `static inline` 零开销抽象：`lv_mutex_t` → CRITICAL_SECTION / pthread_mutex_t，`lv_cond_t` → CONDITION_VARIABLE / pthread_cond_t，`lv_thread_t` → HANDLE / pthread_t，共 18 个 API（init/destroy/lock/unlock/cond 全套/thread create/join/detach/sleep/once） | 扩展新平台只改 1 个文件 |
| **互操作桥接** | `lvInteropPlugin` vtable 结构体（name/version/system/export_proof/import_proof/validate），Coq/Lean4 均已实现统一接口 | 新增后端需改 4 处（枚举值 + 新建 .c + 注册调用 + CMake） |
| **重写引擎** | 策略组合子系统，11 种策略类型，统一 `strategy_execute()` 递归执行入口 | 新增规则从 .lvz 文件加载，无需改 C 代码 |
| **SMT 后端** | 引擎注册表 + dispatch 模式，`SMTBackendEntry` 注册，Groebner 内置，Z3/CVC5/Singular 通过外部进程调用 | 新增后端注册即可 |

---

## 二、需要部分抽象改进的模块（中优先级）

| 模块 | 当前问题 | 改进方案 |
|------|---------|---------|
| **公式渲染器** | 函数指针表 + switch 语句两处存在重复映射关系 | 统一为函数指针表，消除重复 switch |
| **几何压缩** | `cfg.entropy` 配置项在 pipeline 中被忽略，配置不生效 | 修复 pipeline 使其尊重配置参数 |
| **证明导出** | 两套导出层并存（`proof_export.c` 和 `proof_export_enhanced.c`），格式表不统一 | 统一为增强版 dispatch |

---

## 三、需要重点抽象的重构项（高优先级）

### H1: 预设系统注册代码生成

| 属性 | 数据 |
|------|------|
| **涉及文件** | `core/src/layer4_reasoning/preset/` 下 56 个 `preset_xxx.c` 文件 + `core/include/lv/` 下 63 个 `.h` 文件 |
| **问题规模** | 数百处重复注册代码，每个 `preset_xxx.c` 的 `register` 函数都是 9–10 参数的重复调用，约 80–90% 为重复模板 |
| **方案** | 使用 X-macro 或 Python 代码生成器，从 CSV/YAML 描述文件统一生成所有注册代码 |
| **收益** | 消除 56 个文件中的重复模板，收益最大 |

### H2: SIMD 操作真正实现

| 属性 | 数据 |
|------|------|
| **涉及文件** | `simd_ops.h`（58 个向量/矩阵操作函数声明）+ `simd_ops.c`（检测实现 + 全部标量回退） |
| **问题** | `simd_ops.c` 能检测 SSE2/SSE4.1/AVX/AVX2/AVX-512F/NEON，但所有函数均为纯 C 标量回退，无任何 SIMD 内联实现 |
| **方案** | 使用 `#ifdef` 条件编译为 SSE、AVX、NEON 分别添加内联实现 |
| **收益** | 将已检测到的硬件能力真正转化为性能收益 |

### H3: 约束图节点类型 vtable

| 属性 | 数据 |
|------|------|
| **涉及类型** | `GeomType` 枚举 6 种：GEOM_POINT / LINE_SEGMENT / REGION / CIRCLE / PORT / FUNCTION_BLOCK |
| **问题** | `alloc` / `free` / `clone` / `hash` / `compare` / `serialize` 六种操作为每个类型在各文件中独立实现，switch 语句在 4+ 个文件中重复 |
| **方案** | 引入 `GeomNodeVTable` 结构体，以 vtable 消除类型代码反模式 |
| **收益** | 消除 4+ 文件中的重复 switch，新增节点类型只需实现 vtable |

### H4: 引擎调度器全局状态消除

| 属性 | 数据 |
|------|------|
| **涉及文件** | `engine_scheduler.c` |
| **问题** | 存在 `g_compat_engine` 和 `g_default_scheduler` 全局静态变量，与 v3.3.0+ 隔离上下文目标冲突 |
| **方案** | 将调度器实例嵌入 `lvEngine`，后端分发改用注册表 |

### H5: 内存分配器策略接口

| 属性 | 数据 |
|------|------|
| **涉及文件** | `lv_malloc` 相关实现 |
| **问题** | 当前为轻量封装，无可替换策略 |
| **方案** | 引入 `AllocatorOps` vtable，支持调试池 / 生产池 / 外部分配器切换 |

---

## 四、优先级排序建议

### 高优先级（立即着手）

| 序号 | 项 | 原因 |
|------|----|------|
| 1 | **H1 预设系统代码生成** | 消除 56 个文件的重复模板，收益最大，影响面最广 |
| 2 | **H2 SIMD 真正实现** | 检测到的硬件能力不利用是纯粹的性能浪费 |
| 3 | **H3 约束图节点 vtable** | 消除 4+ 文件中的重复 switch，改善可维护性 |

### 中优先级（下一轮）

| 序号 | 项 |
|------|-----|
| 4 | H4 引擎调度器全局状态 |
| 5 | H5 内存分配器策略接口 |
| 6 | 公式渲染器 dispatch 统一 |
| 7 | 几何压缩配置修复 |

### 低优先级（后续优化）

| 序号 | 项 |
|------|-----|
| 8 | 证明导出层统一 |
| 9 | 引擎流水线步骤描述符数组 |
| 10 | 引擎状态码表驱动 |

---

## 五、工作估算

| 优先级 | 项数 | 预估工作量 | 影响范围 |
|--------|------|-----------|---------|
| 高 | 3 | 2–3 周 | 跨 60+ 文件，核心性能与可维护性 |
| 中 | 4 | 1–2 周 | 局部模块改进 |
| 低 | 3 | 3–5 天 | 清理与标准化 |

---

*本报告基于对代码库的审计数据整理，仅反映抽象设计层面的分析，不涉及功能正确性评估。*